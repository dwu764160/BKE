TASK: Step 2 (generative possession engine) — BUILD DONE, both modes PASS. Next = FORK repos (Core + markets + game) per docs/phase_v1_architecture.md.
STATUS: 2026-05-31. All three Step-2 deliverables built, run clean, artifacts written, pytest 3/3. NOT yet committed (awaiting user go).

STEP 2 BUILD RESULT:
- src/simulation/possession_engine.py: PossessionResolver seam +
  OutcomeSamplerResolver (Core sampler, single-poss resolve() + vectorized
  simulate_team_batch()) + RateModel (explainable spine: base behavioral_* +
  pts_v40 talent x archetype_shape x Step-1 matchup_mult, FULL mismatch magnitude)
  + BoxScoreAggregator. PossessionOutcome rich schema so game-track MarkovEventResolver
  drops in on the same seam with no schema change.
- scripts/run_possession_engine.py: --mode v0 (5 starters + aggregate bench unit,
  covers 240 min) | --mode v1 (top 9-11 by projected MPG, normalized to 240).
  Calibrates global constants on train <=2023-24 (pace/oreb/league_ft/league_3p +
  single efg_scale to match train team PTS); applies forward to 2024-25.
  -> data/processed/simulation/possession_box_distributions.parquet +
     possession_engine_constants.json
- scripts/validate_possession_engine.py: BOTH gates, 2024-25 holdout ->
  reports/possession_engine_validation.json (+ _v0/_v1 snapshots).

VALIDATION (walk-forward, both PASS) — authoritative production run:
  v0: TEAM mae=10.21 bias=+1.08 cov=0.890 | PLAYER n=7274  pts_mae=6.65 ast=1.98 reb=2.74 cov=0.642
  v1: TEAM mae=10.34 bias=+1.86 cov=0.885 | PLAYER n=13173 pts_mae=5.89 ast=2.09 reb=2.46 cov=0.649
  v1 is the better prop model (pts_mae 5.89<6.65, 13173>7274 scorable games). Sim team ~115 vs actual ~114.

KNOWN LIMITATION (deferred to Step 2.1): player-points P10-P90 coverage 0.65 < 0.80
target (intervals slightly narrow). Dominant missing variance = MINUTES (v1 holds
projected MPG fixed; no DNP/foul-trouble/blowout minute swings). Add per-game
minutes dispersion + the deferred foul-out/OT/clutch logic first.

BUGS FIXED DURING BUILD (see docs/findings/possession_engine_step2_2026-05-31.md):
1. DREB almost never recorded (vectorized miss branch else-bug); fixed -> reb_mae 4.2->2.4.
2. team_game_logs is a COMBINED file (SEASON col), not per-season; calibration was
   silently using defaults. 3. player logs have both Player_ID & PLAYER_ID -> dedup on upper.

ENV NOTE: tool-output DISPLAY lagged/stalled badly this session (OneDrive Files-On-
Demand). Execution was fine; verified via /tmp file reads + unique-marker greps. See
[[reference_bke_data_schemas]].

NEXT — FORK (docs/phase_v1_architecture.md sequencing step 3):
- Markets (existing Robinhood/Kalshi repo, Sleeve C): consume
  possession_box_distributions.parquet -> price props/totals; override projected
  starters with live actuals+injuries at T-60/30. Never runs the resolver.
- Game (NEW repo): implement MarkovEventResolver (full event-level sim + rich
  per-player tendency model) on the existing PossessionResolver seam.
- Step 2.1 (Core, before/around fork): minutes dispersion to lift prop coverage to ~0.80.
