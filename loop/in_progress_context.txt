TASK: PTS→game-model wiring — Phase 1/2 DONE, awaiting decision on Phase 3
STATUS: 2026-05-30. Committed+pushed prior work (76dd17a). Built+validated PTS
team ratings. Decisive result: lagged PTS does NOT help the game model.

DELIVERABLES (this session):
- scripts/build_pts_team_ratings.py -> data/processed/forecast/team_pts_ratings.parquet
  (270 team-seasons, 9 seasons). Variants A (PTS-only additive) + B (PTS+fit).
  C/D skipped: repo Lasso already found archetype-pair interactions negligible
  (INTERACTION_MATRIX={}, team_feature_aggregation.py:167-196) => C/D ≈ B.
- scripts/validate_pts_team_ratings.py (2 performance gates).
- docs/findings/pts_wiring_phase1_2026-05-30.md (full result).

RESULTS:
- Gate 1: pts_net ↔ same-season actual net rating r=0.840 (EXCELLENT metric).
  BUT as a prior-year prior: pts_net 0.514 < margin 0.542 (loses). Stale carry.
- Gate 2 (walk-forward, 8279 OOS games, lagged N-1 PTS as GBDT feature):
  Elo 0.2193 | GBDT base 0.2250 | +PTS 0.2260 | +PTS+fit 0.2256.
  PTS vs GBDT-base = NOISE (Δ+0.0009, CI straddles 0); PTS vs Elo = worse.
  => Lagged PTS adds NOTHING. Do NOT promote.

INTERPRETATION: metric is good (r=0.84 contemporaneous), but the only leakage-free
wiring today is a 1-season lag, which Elo + prior-year margin already subsume.
The unlock = YTD/current-roster PTS (recompute PTS from games-to-date on the
CURRENT roster) — can't test with season-aggregated PTS (would leak). Needs the
Phase-3 data track: per-game availability/inactives + incremental PTS. That's a
multi-step data-engineering effort, NOT wiring.

BOTTLENECK CONFIRMED: it's DATA (current roster + availability), not the metric.

DECISION POINT FOR USER: invest in Phase 3 (game-day inactives fetch + YTD PTS
machinery + preseason-roster backfill to 8 seasons)? Only path for player-impact
to beat Elo. Large effort. Awaiting go/no-go.

NOT DONE: Phase 3 (parallel data track) — paused pending user decision given
Gate 2 null result.
