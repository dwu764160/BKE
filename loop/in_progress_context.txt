TASK: Step 1 (cross-team matchup engine) — BUILD DONE. Next = Step 2 (generative stat-line / possession model).
STATUS: 2026-05-31. All three Step-1 deliverables built, run clean, artifacts written.

STEP 1 BUILD RESULT:
1. scripts/fit_archetype_interactions_v2.py
   - EB-shrinks the locked 8-season FE cells (reports/archetype_interaction_signal_test.json,
     99 cells) toward 0 using per-cell SE (from the 95% CI, which encodes poss),
     then enforces poss-weighted mean-zero per offensive archetype.
   - Out: data/processed/bke/cross_team_interactions_matrix.parquet (shrunk +
     provenance: raw_ppp, se, shrink_factor, poss, ci, source=emergent_matchup).
   - Run: 99 cells, shrunk std ~0.040 ppp, mean shrink_factor ~0.92, mean-zero
     residual ~4.5e-18 (constraint holds). Also exposes estimate_fe_cells() — a
     reusable two-way-FE estimator with a season filter, used by validation.
2. scripts/compute_matchup_adj.py — the 3-pair engine.
   - Pair1 perimeter threat->POA/Wing Stopper; Pair2 interior threat->big-bodied
     defender (sub-case A) or center overloaded (sub-case B) + rim-anchor paint
     term; Pairs3-5 band rank-to-rank + discounted mismatch adj (0.5x: diff0
     -0.010, diff1 +0.005, diff2 +0.020). argmax cell lookup; band-avg fallback
     for Unknown off arch. Talent rank = pts_o_v40 / pts_d_v40.
   - Joins per-player attrs (archetype/position_band_3/position_proxy/mpg) from
     projected_player_profiles (fallback player_impact_profiles); starters from
     simulation_step2_lineup_profiles.starter_player_ids (a JSON string).
   - Out: data/processed/bke/cross_team_interactions.parquet, schema per design.
   - Granularity = (season, team, opponent), game_id=NULL: matchup source is
     SEASON-aggregated (no GAME_ID) and projected lineups are season-level, so a
     team-pair matchup is identical across their games. Markets track fans out
     per-game later with LIVE lineups.
   - Run: 7380 rows / 8 seasons; center_overloaded 0.570; pair1 argmax-resolved
     0.923; lineup_net_rating_adj mean -0.26, std 1.32, range [-4.27,+5.59] pts/100.
3. scripts/validate_matchup_interactions.py — walk-forward, leakage-free.
   - Fit two-way FE + cells on train (<=2023-24), EB-shrink, apply forward to
     2024-25 matchup rows. Compare poss-weighted player-game PPP MAE: baseline
     (grand+off_fe+def_fe) vs +interaction[off_arch][def_arch]. Paired bootstrap.
   - Out: reports/cross_team_interaction_validation.json.
   - Run (archetypes from player_impact_profiles, actual, all 8 seasons):
     n=105,595 holdout rows; overall WMAE 0.18256->0.18119 (dWMAE -0.00138, +0.75%);
     significant-cell stratum (n=69,980) -0.00278; paired bootstrap p(model
     better)=1.000, CI[-0.00164,-0.00112]. VERDICT: MODEL HELPS (OOS), leakage-free.
     Bigger gain on significant cells = the expected pattern. (Note: an interim run
     using sparse projected_player_profiles archetypes was NEUTRAL; actual-archetype
     coverage is what unlocks the OOS win.)

DOC/REGISTRY UPDATES: docs/multiple-versions.md (new "Cross-Team Interaction
Matrix" section); docs/findings/matchup_engine_step1_2026-05-31.md (build writeup).

KEY DATA-SCHEMA TRUTHS (verified this session — prior notes had display-corrupted
schemas):
- league_season_matchups.parquet is SEASON-aggregated per off x def player pair;
  cols incl SEASON, OFF/DEF_PLAYER_ID, PARTIAL_POSS, PLAYER_PTS, GP. NO game_id,
  NO team_id.
- simulation_step2_lineup_profiles.parquet carries only starter_player_ids /
  clutch_player_ids at player level (plus team mu/sigma). No per-player archetypes.
- projected_player_profiles.parquet HAS position_band_3 (smalls/wings/bigs),
  position_proxy, off/def_primary_archetype, mpg; seasons 2018-19..2024-25.
- pts_v40.parquet keys = player_id+season; cols pts_o_v40, pts_d_v40 (+v32/a/c).

NEXT — STEP 2 (generative stat-line / bottom-up possession engine):
- Consume cross_team_interactions.parquet (lineup_net_rating_adj + per-player
  player_matchup_adjs + center_overloaded) as a possession-engine input.
- Use FULL empirical mismatch magnitude (not the 0.5x projection discount) where
  real-time mismatch hunting is simulated.
- After Step 2, FORK repos (Core + markets + game) per docs/phase_v1_architecture.md.
