DO NOT CHANGE — IN-PROGRESS CONTEXT (Machine-Readable Guidance)

Purpose:
- This file captures the full working context when a model or session encounters difficulty.
- It is intended to be written to by models that are failing on a task so other models can pick up
  where they left off. Do not manually edit this file unless you are coordinating the handoff.

CONTEXT:
- Task: BKE v2.6 — Bayesian shrinkage, backtesting framework, L4 fix, system audit
- Env: Linux (WSL), venv at `.venv/`, Python 3.10
- Key files modified this session:
  - `src/modeling/bayesian_hierarchical.py` — Fixed shrinkage calibration (was 50% for all players, now ~1-5% based on GP*reliability)
  - `src/modeling/decomposition_engine.py` — L4 scheme stability now bonus-only (clipped to >=0 in TI formula)
  - `src/modeling/backtesting.py` — Created and run, all tests pass
- Status: ✅ All three deferred features implemented and validated
- Pipeline: 950 qualified, ~12s runtime, 344 columns

COMPLETED:
- [x] Bayesian shrinkage calibration fixed (observation noise now scales with GP*reliability)
- [x] Backtesting framework created and run (PTS ρ=0.553, TI within-1=76.0%)
- [x] L4 scheme stability fix (bonus-only, no penalty for stars)
- [x] Full system audit written (loop/system_audit_v2.6.md)
- [x] Context files updated

KNOWN ISSUES (from audit):
1. Layer 3 elevation inflates role players (Gafford #3, Pritchard #5 in 2024-25)
2. RUE favors simple roles over complex ones
3. Playmaking creation dimension unstable (ρ=0.297 year-over-year)
4. RAPM noise for players on bad teams (LeBron -5.16 in 2024-25)
5. "Unknown" player appearing in bottom-5 (data quality issue)

NEXT_STEPS:
- Cap L3 elevation per archetype (max 1.5σ) — highest-priority fix
- Add role complexity weighting to RUE
- Stabilize playmaking creation dimension (multi-season smoothing)
- Apply Bayesian shrinkage to RAPM directly
- Incorporate unused tracking data into defensive dimensions
- Reduce L3 weight from 20% to 12-15%
