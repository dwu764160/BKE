2026-05-19 — Current simulation-risk note
-------------------------------------------------------------
- The latest player-stat/simulation patch set still has a known reconciliation weakness: `src/simulation/player_stats_sim.py` and the mirrored frontend in `app/simulation_viewer.py` can overshoot the requested team score in rare zero-FT correction paths.
- Reproduced in a 50-seed smoke test (seed 5 -> 114 points on a 112-point target).
- Test coverage gap: `tests/test_simulation_core.py` currently misses that branch because the schema assertion checks the fixture roster rather than the simulated box output.
- Status: documentation has been updated to reflect the issue, but the change set should not be described as fully push-safe yet.

2026-03-11 — Player stat realism upgrade: team budgets + usage competition + softer archetype effects
-------------------------------------------------------------------------------------------------------------
- Reworked `src/simulation/player_stats_sim.py` so single-game/detailed-season player stats now flow through:
	- team stat budgets first (`FGA`, `3PA`, `FTA`, `TOV`, rebound/stock proxies),
	- capped Dirichlet usage competition (starter-boosted, star-variance aware),
	- multinomial player allocation that conserves team totals,
	- Beta-Binomial shooting for hot/cold variance,
	- softer archetype matchup effects (`PLAYER_GAME_ARCHETYPE_EFFECT_SCALE = 0.60`).
- Added margin-aware game script adjustments:
	- close games push more minutes and usage into clutch cores,
	- blowouts trim starter minutes and redistribute to bench rotation players.
- Viewer sync:
	- `app/simulation_viewer.py` single-game JS now mirrors the same structure (team totals first, usage competition second, constrained allocation third), so the frontend no longer drifts from the backend stat engine.
- New config knobs in `src/simulation/simulation_config.py`:
	- `PLAYER_GAME_ARCHETYPE_EFFECT_SCALE = 0.60`
	- `PLAYER_GAME_STARTER_USAGE_BOOST = 1.18`
	- `PLAYER_GAME_CLUTCH_USAGE_BOOST = 1.06`
	- `PLAYER_GAME_USAGE_CAP = 0.38`
	- `PLAYER_GAME_DIRICHLET_SCALE = 85.0`
	- `PLAYER_GAME_BETA_CONCENTRATION = 42.0`
	- `PLAYER_GAME_BLOWOUT_MARGIN = 15.0`
	- `PLAYER_GAME_BLOWOUT_STARTER_PENALTY = 0.10`
- Validation:
	- `pytest -q tests/test_simulation_core.py` → 3/3 pass
	- `python3 scripts/validate_player_stat_sim.py --mode backtest`
		- PTS: `MAE=0.92`, `r=0.987`, `bias=+0.48`
		- AST: `MAE=0.22`, `r=0.989`
		- REB: `MAE=0.39`, `r=0.986`
		- FGA: `MAE=0.72`, `r=0.988`
		- FG3A: `MAE=0.33`, `r=0.982`
- Result: realism constraints improved structurally without degrading season-level validation versus the prior pass.

2026-03-11 — Grid sweep calibration: tuned LINEUP_TEAM_BONUS_CLIP, MATCHUP_TEAM_INTERACTION_SCALE, PPP_CONTEXT_BONUS_SCALE
-------------------------------------------------------------------------------------------------------------
- Ran 64-trial grid sweep across 3 sensitive knobs (scripts/grid_sweep_sim_knobs.py):
	- LINEUP_TEAM_BONUS_CLIP: [0.30, 0.40, 0.55, 0.70]
	- MATCHUP_TEAM_INTERACTION_SCALE: [0.15, 0.25, 0.35, 0.50]
	- PPP_CONTEXT_BONUS_SCALE: [0.0, 0.0015, 0.0025, 0.005]
- Best combo by combined margin+PPP MAE: CLIP=0.30, MATCHUP=0.15, PPP_CTX=0.0
- Applied to `src/simulation/simulation_config.py`:
	- LINEUP_TEAM_BONUS_CLIP: 0.55 → 0.30
	- MATCHUP_SPREAD_BONUS_CLIP: 0.35 → 0.15
	- MATCHUP_TEAM_INTERACTION_SCALE: 0.35 → 0.15
	- PPP_CONTEXT_BONUS_SCALE: 0.0025 → 0.0
- Backtest results (10k sims) after tuning:
	- 2022-23 margin: MAE=5.47, RMSE=6.74, r=0.8305 (was 5.52/6.81/0.827)
	- 2023-24 margin: MAE=5.93, RMSE=7.15, r=0.8683 (was 5.94/7.15/0.868)
	- 2024-25 margin: MAE=3.71, RMSE=4.42, r=0.9480 (was 3.70/4.44/0.947)
	- 2022-23 ppp: MAE=5.27, RMSE=6.17, r=0.8316 (was 5.33/6.22/0.824)
	- 2024-25 ppp: MAE=6.19, RMSE=7.41, r=0.9408 (was 6.32/7.51/0.936)
- Forecast results (end_of_season scenario):
	- 2023-24 margin: MAE=7.69, RMSE=9.74, r=0.7053 (was 7.75/9.80/0.703)
	- 2024-25 margin: MAE=8.85, r=0.6196 (neutral)
- Conclusion: net improvement across most season/model pairs; confirms lineup/matchup channels
  are properly marginal and lower interaction scales reduce noise without losing signal.

2026-03-10 — Simulation core rework: Step 1/Step 2 integration + matchup/player-stat backend + calibration pass
-------------------------------------------------------------------------------------------------------------
- Completed a backend-only simulation-core rework across `src/simulation/` with four major changes:
	- `season_sim.py` now reuses shared lineup-projection context internally instead of ignoring Step 2 logic.
	- Added archetype-vs-archetype interaction handling for main rotation units via new `src/simulation/player_stats_sim.py`.
	- Added detailed single-game and sample-season player stat simulation outputs.
	- Upgraded `lineup_projection.py` with continuity priors, lineup-fit heuristics, and candidate-pool pruning.
- New outputs:
	- `data/processed/simulation/simulation_step1_player_season_stats.parquet`
	- `data/processed/simulation/simulation_step1_player_game_samples.parquet`
	- `data/processed/simulation/forecast_step1_player_season_stats_<scenario>.parquet`
	- `data/processed/simulation/forecast_step1_player_game_samples_<scenario>.parquet`
	- `reports/simulation_single_game.json`
	- `reports/forecast_single_game.json`
- Documentation synced:
	- `readme.md` updated for new simulation outputs, single-game CLI, and shared Step 1/Step 2 context usage.
	- `src/simulation/run_forecast.py` now records scenario-specific player season/game artifact paths in its output map.

2026-03-09 — compute_linear_metrics logic fix: DWS normalization target + net proxy correction
-------------------------------------------------------------------------------------------------------------
- Continued deep audit of remaining `src/data_compute` scripts identified a high-confidence active logic bug in:
	- `src/data_compute/compute_linear_metrics.py`
		- DWS normalization math did not enforce stated season WS target (`1230`), causing systematic WS inflation.
		- Team net proxy helper incorrectly divided `PLUS_MINUS_PER_GAME` by `GAMES` (double-normalization).
- Fixes implemented:
	- DWS normalization now scales to `target_dws = max(1230 - total_ows, 0)` per season.
	- `load_team_net_ratings()` now uses `PLUS_MINUS_PER_GAME` directly.
	- Replaced deprecated `groupby.apply` for `L_PPP` with explicit grouped aggregation (future-safe behavior, no logic change).
- Validation:
	- `python3 src/data_compute/compute_linear_metrics.py`
	- Before -> after total WS by season:
		- 2022-23: `1301.6 -> 1230.0`
		- 2023-24: `1307.7 -> 1230.0`
		- 2024-25: `1309.9 -> 1230.0`
	- Script reruns cleanly, and output saved to `data/processed/metrics_linear.parquet`.

2026-03-09 — Projection carry guardrail tuning + simulation validation null-handling fix
-------------------------------------------------------------------------------------------------------------
- Deep audit continuation across `src/player_eval`, `src/simulation`, `src/data_compute`, and `src/modeling` identified two high-confidence logic fixes with measurable/behavioral impact:
	- `src/player_eval/project_next_season.py`
		- `fit_minutes_carry_model(...)` fallback guardrail relaxed from `corr < 0.80` to `corr < 0.75`.
		- Rationale: current historical fit quality is consistently ~0.774, so old cutoff disabled learned carry coefficients in all runs and forced fallback-only behavior.
	- `src/simulation/validate_sim.py`
		- Fixed falsy check for margin metrics serialization:
			- `margin_rmse` / `margin_mae` now use `is not None` guard, so true zero values are no longer emitted as `null`.
- Documentation sync:
	- Updated `readme.md` Forecast Stage 1 description to document carry-anchor acceptance threshold (`>= 0.75`).
- Validation and impact (controlled before/after, same data and settings):
	- Projection quality (`team_mapping_mode=end_of_season`):
		- 2022-23 -> 2023-24 MPG MAE: `3.9834 -> 3.9125` (delta `-0.0710`)
		- 2023-24 -> 2024-25 MPG MAE: `4.2281 -> 4.1436` (delta `-0.0845`)
		- Average MPG MAE: `4.1058 -> 4.0280` (delta `-0.0778`, ~`1.9%` better)
		- Average MPG correlation: `0.8394 -> 0.8396` (slight improvement)
	- Full rerun confirms carry model now activates in both transitions:
		- `n=539, r=0.7744` and `n=1200, r=0.7736`.
	- Simulation validation rerun completed successfully (`python3 src/simulation/validate_sim.py`), output written to `reports/simulation_step1_validation.json`.

2026-03-08 — Team summary crash fix (mixed TEAM_ID types)
-------------------------------------------------------------------------------------------------------------
- Continued deep review identified a deterministic crash path in `src/data_fetch/summarize_team_logs.py`:
	- `main()` resolved team names via `str(int(float(tid)))`, which raises `ValueError` when padded/team-derived `TEAM_ID` values are abbreviations (e.g., `ATL`).
	- `build_padded_team_games(...)` also used strict float casting for `TEAM_ID`, which could fail on non-numeric identifiers.
- Fixes implemented:
	- Robust `TEAM_ID` normalization in `build_padded_team_games(...)` (numeric IDs normalized when possible; abbreviation tokens preserved).
	- Added safe `resolve_team_name(...)` in `main()` supporting both numeric team IDs and team abbreviations.
	- Added explicit abbreviation-to-name mapping for canonical 30 teams.
- Validation:
	- `get_errors` clean for `summarize_team_logs.py`.
	- Runtime smoke test passed:
		- `python3 src/data_fetch/summarize_team_logs.py`
		- Wrote `team_game_details` and `team_summaries` outputs successfully (no TEAM_ID conversion crash).
- Documentation synced:
	- `readme.md` summarize command annotation updated for mixed TEAM_ID handling.

2026-03-08 — Fetch parser payload-shape hardening sweep (remaining ingest scripts)
-------------------------------------------------------------------------------------------------------------
- Continued file-by-file logic audit and fixed remaining deterministic parser fragility in fetch stage.
- Root issue pattern: several scripts still directly indexed `json_data['resultSets'][0]` or only accepted one payload shape.
- Updated scripts:
	- `src/data_fetch/fetch_box_scores_complete.py`
		- Added payload extractor and fallback parsing for both `resultSets` and `resultSet`.
	- `src/data_fetch/fetch_official_stats.py`
		- Added payload extractor for both shapes.
		- Corrected request param key casing from `PorRound` -> `PORound` for consistency with endpoint conventions.
	- `src/data_fetch/fetch_defensive_metrics.py`
		- Cache validation + parser now accept both payload shapes.
	- `src/data_fetch/fetch_shot_zones.py`
		- Cache validation + parser selection now accept both payload shapes.
	- `src/data_fetch/fetch_player_draft_history.py`
		- Direct HTTP DraftHistory path now accepts both payload shapes before fallback to `nba_api` wrapper.
	- `src/data_fetch/fetch_pbp/fetch_play_by_play.py`
		- `__NEXT_DATA__` parser now accepts `resultSets`/`resultSet` and avoids empty-list indexing failure.
- Documentation synced:
	- `readme.md` fetch-stage command notes updated for the above payload compatibility behavior.
- Validation:
	- `get_errors` clean for all edited scripts.
	- Synthetic payload-shape smoke tests passed for box/offical/defensive/shot-zones/draft-history helpers.
	- PBP synthetic import test could not run in this environment because `playwright` is not installed; static diagnostics for file are clean.

2026-03-08 — Fetch-stage logic hardening: player ID-name map resolution
-------------------------------------------------------------------------------------------------------------
- Continued user-requested logic audit (function-level, not runtime-only) and found deterministic mapping bugs in fetch stage:
	- `src/data_fetch/fetch_historical_data.py` loaded player-name map from a hardcoded malformed filename (`player_id_name_map_['2022-23', '2023-24', '2024-25'].csv`) during player game-log fetch.
	- `src/data_fetch/fetch_player_salaries.py` used the same malformed hardcoded filename for ID/name matching.
- Fixes implemented:
	- Added robust season-aware map resolver in `fetch_historical_data.py` with ordered fallback:
		1) `player_id_name_map_<season>.csv`
		2) `player_id_name_map_all.csv`
		3) legacy malformed filename
		4) any `player_id_name_map_*.csv`
	- Added `_load_player_id_name_map()` in `fetch_player_salaries.py` that aggregates all available map files (season-first, legacy-compatible), dedupes by `PERSON_ID`, and fails fast with actionable message when missing.
	- Hardened `save_player_id_name_mapping(...)` to accept iterable seasons safely (writes per-season files instead of producing malformed list-literal filename).
	- Hardened JSON response parsing in `fetch_tracking_data.py` and `fetch_matchup_data.py` to accept both `resultSets` and `resultSet` payload shapes (including cached responses), preventing silent empty DataFrames when endpoint response format varies.
	- Hardened `fetch_players.py` with `try/finally` connection lifecycle so early exits (missing parquet / missing `PLAYER_ID`) always close SQLite cleanly.
- Documentation updated in `readme.md`:
	- Fetch-stage comments now describe season map generation/consumption.
	- Data layout now explicitly documents `player_id_name_map_<season>.csv` and optional compatibility map files.
	- Tracking/matchup command notes now include payload-format compatibility behavior.
	- `fetch_players.py` command note now reflects safe DB close behavior.

2026-03-08 — Pipeline logic-audit pass (function-level) + high-impact fixes
-------------------------------------------------------------------------------------------------------------
- User request shifted from runtime checks to logic-error audit across pipeline scripts.
- Confirmed and fixed multiple logic defects discovered by direct function inspection:
	- `src/player_eval/project_next_season.py`
		- `fit_minutes_carry_model(...)` output was computed but never used in `project_minutes(...)`.
		- Fix: added carry-anchor blend inside `project_minutes(...)` and passed `minutes_carry_model` from `project_season(...)`.
	- `src/simulation/game_model.py`
		- `generate_balanced_schedule(...)` was not truly balanced: created 1305 games then truncated to 1230, yielding team totals 66-87 and home splits 26-58.
		- Fix: replaced with exact construction (double round-robin base + balanced extra-pair graph + home/away orientation), no truncation.
	- `src/profile_aggregate/team_feature_aggregation.py`
		- Backtest aggregation merged minute predictions but still used profile actual minutes/MPG (Step 2 output effectively ignored).
		- Fix: backtest now uses Step 2 `pred_mpg_raw` (fallbacks preserved), recomputes minutes from predicted MPG*games, and merges with stable keys.
	- `src/simulation/lineup_projection.py`
		- Forecast mode used global constant mutation to redirect profile path.
		- Fix: added explicit `source_path` parameter to `load_player_pool(...)`; removed global mutation hack.
		- Updated Step 2 report `validation_targets.rotation` text to match implemented bench-player impact methodology.
	- `src/simulation/season_sim.py`
		- PPP summary `ppp_net_rating`/`mu` used inconsistent raw `off-def` display vs calibrated simulation math.
		- Fix: summary net now uses calibrated PPP-equivalent net-rating transform aligned with model blend/scale.
	- `src/player_eval/train_minute_model.py`
		- Added compatibility wrapper to preserve documented pipeline command path (canonical implementation remains in `src/simulation/train_minute_model.py`).
	- `app/simulation_viewer.py`
		- Updated modal labels from "bench-heavy" to "bench-player impact" to match backend validation target.
- `readme.md` updated for this pass:
	- Step 3 minute-source behavior note (backtest uses Step 2 predicted MPG fallback chain).
	- Forecast Step 1 minute-normalization wording updated (roster-adaptive target derived from team_mpg_target=350).
	- Forecast schedule note updated to reflect fixed no-truncation balanced construction.

2026-03-XX — MPG forecast fix + rotation correlation improvement
-------------------------------------------------------------------------------------------------------------
- MAJOR FIX: MPG projection compression resolved.
  ROOT CAUSE: normalization target was 240 (physical on-court limit) but actual data
  uses per-stint MPG values that sum to ~350 per team. This 30-35% scale mismatch
  was compressing all projected minutes, making stars project at 25-28 MPG instead of 33-37.
- Solution: replaced additive-delta + carry-model with multiplicative minute-share approach.
  Normalization target changed from fixed 240 to roster-size-adaptive (n_players * 18.5).
- Results:
  - MPG correlation: 0.783/0.793 → 0.846/0.834
  - MPG MAE: 8.3+/9.1+ → 4.02/4.25
  - Players >30 MPG: 14/10 → 105/98 (actual: 98/100)
  - Mean projected MPG: 12.6 → 18.5 (actual: 18.7/19.3)
- Rotation correlation improved: 0.448 → 0.684 by changing actual target from noisy
  lineup-level NET_RTG (<=2 starters, >=50 poss) to player-level impact_total_impact
  of non-starter players from game-log-derived profiles.
  Per-season: 2023-24=0.531, 2024-25=0.808.
- Draft position bonus added to minute projection (top-5: 1.06x, top-14: 1.04x, 1st round: 1.02x).
- Frontend labels updated for new rotation methodology.
- Full pipeline regenerated: project_next_season → run_forecast → simulation_viewer.

2026-03-08 — Forecast scenario integrity fix + replacement exclusion hardening + minutes carry stabilization
-------------------------------------------------------------------------------------------------------------
- User-requested follow-up completed across backend + frontend + docs.
- Core forecast integrity fixes in `src/player_eval/project_next_season.py`:
	- Removed preseason salary-team fallback path; when preseason snapshots are missing, mapping now explicitly falls back to carry-forward teams.
	- Added minutes carry-model fitting (`fit_minutes_carry_model`) and blended carry-anchor minutes projection with guardrails (fallback coefficients when fit is weak).
	- Added robust replacement-pool row detection and exclusion before final projected outputs.
- Replacement exclusion now hardened across downstream forecast consumers:
	- `src/profile_aggregate/team_feature_aggregation.py`
	- `src/simulation/season_sim.py`
	- `src/simulation/lineup_projection.py`
- Forecast frontend parity/integrity updates in `app/simulation_viewer.py`:
	- Hardened scenario merge logic to avoid stale mixed payloads.
	- Added deterministic scenario ordering (`end_of_season`, `preseason_snapshot`).
	- Expanded Step 2 Forecast modal/cards with backtest-style validation detail (overlaps, rotation predicted/actual, observed starter/clutch names, starter-target source coverage).
- Config update:
	- `src/player_eval/constants.py`: `ENABLE_FORECAST_REPLACEMENT_BUFFER=False`.
- Validation + regeneration completed:
	- `python3 src/simulation/run_forecast.py --skip-preseason-fetch`
	- `python3 app/simulation_viewer.py`
	- `get_errors` clean for all modified source files.
- Confirmed case-study behavior (2024-25 GSW/Jimmy):
	- End-of-season scenario: Jimmy Butler III -> GSW, GSW projected wins `63.5`.
	- Preseason snapshot scenario: Jimmy Butler III -> MIA (`team_mapping_source=carry_forward_fallback`), GSW projected wins `49.7`.
	- Scenario toggle now reflects real divergence instead of prior near-identical outputs.
- Latest carry validation snapshot:
	- End-of-season MPG r: `0.7833`, `0.7932`.
	- Preseason MPG r: `0.7342`, `0.7301`.
- NOTE: This entry supersedes earlier same-day notes that described active preseason salary-team fallback and active replacement-buffer contribution in final forecast calculations.

2026-03-08 — Step 2 archetype-gate removal + forecast Step 2 split + validation parity
--------------------------------------------------------------------------------------
- Implemented user-requested removal of Step 2 archetype gate filtering in both backtest and forecast:
	- `src/simulation/lineup_projection.py`: removed lineup-pool exclusion of `Insufficient Minutes` archetype rows.
	- `src/simulation/simulation_config.py`: removed obsolete gate constant `STEP2_EXCLUDE_INSUFFICIENT_MINUTES_ARCHETYPES`.
- Forecast Step 2 validation now mirrors backtest target construction while keeping forecast scoring leakage-safe:
	- Forecast scoring path still uses no clutch-minute scoring inputs.
	- Validation path now loads/uses observed targets (Q1 starters from PBP, top-5 clutch minutes, rotation lineups with `total_poss >= 50` and overlap `<= 2`).
	- This parity is implemented in `src/simulation/lineup_projection.py` by splitting forecast clutch usage into `clutch_model` (empty) vs `clutch_validation` (observed targets).
- Frontend forecast view split into two top-level tabs in `app/simulation_viewer.py`:
	- `Step 1 Forecast` (season simulation forecast outputs)
	- `Step 2 Forecast` (forecast lineup projection outputs)
	- Added dedicated Step 2 forecast scenario/season controls and synchronized forecast state across both tabs.
	- Added Step 2 forecast validation cards/team rows to match backtest Step 2 style (`Starter/Clutch/Rotation` validation signals).
- Rebuilt artifacts and validated outputs:
	- Backtest Step 2 rerun completed; summary metrics remained healthy.
	- Forecast scenario rerun completed for `end_of_season` and `preseason_snapshot`.
	- Verified `reports/forecast_lineup_profiles.json` contains populated scenario-level validation overall metrics.
	- Regenerated `app/simulation.html` from updated viewer template.
- Documentation updated:
	- `readme.md` now documents split forecast tabs and forecast Step 2 validation parity.
	- `reference/simulation/Simulation_Core_Summary.md` appended with this pass details.
- NOTE: This entry supersedes earlier same-day notes that described an active Step 2 archetype gate.

2026-03-08 — Simulation Step 2 constraint tuning + rotation alpha optimization + forecast lineup deployment
-----------------------------------------------------------------------------------------------------------
- Implemented missing constraint tuning features in `src/simulation/lineup_projection.py`:
  - Archetype gate: filters "Insufficient Minutes" players from lineup pool
  - Low-minutes penalty: divides starter_score by 1.10 for players under 24 MPG
  - True-big requirement: starter lineup must include at least one Forward-Center or Center
  - Added 5 new fields to `Step2Config` dataclass and corresponding imports from `simulation_config.py`
  - Modified `_select_best_lineup()` to accept optional `require_position_bands` parameter
- Rotation correlation optimization in `src/simulation/simulation_config.py`:
  - Changed `ROTATION_ALPHA` from 0.55 to 0.30 (grid search identified optimal range 0.25-0.35)
  - Disabled rotation regime model (`STEP2_ENABLE_ROTATION_REGIME_MODEL = False`) — regime model hurt correlation by ~0.03
- Ran production backtest: starter=0.7178, clutch=0.86, rotation_corr=0.6177
  - Improvement over TRUE baseline (0.6956/0.8578/0.553): starter +3.2%, clutch +0.3%, rotation +11.7%
  - NOTE: Prior context_summary claimed baseline was 0.7267/0.8667/0.6048 — this was INCORRECT; verified by git stash + clean run
- Ran forecast pipeline: `python3 src/simulation/run_forecast.py --skip-preseason-fetch` (45.3s)
  - Both scenarios (end_of_season, preseason_snapshot) generated lineup profiles for 60 teams each
  - Combined output at `reports/forecast_lineup_profiles.json`
- Frontend fixes (from prior session, validated this session):
  - JS nesting bug fix: extracted `initForecastConferenceTabs()` from inside `switchForecastSeason()`
  - Added Step 2 conference filter tabs
  - Added Forecast Step 2 lineup panel (7 new JS functions, conference filter, card grid, modals)
  - Forecast mode scoring: reversed clutch logic (no current-season leakage), bifurcated weights
- HTML regenerated: `python3 app/simulation_viewer.py` → `app/simulation.html`
- Frontend validation:
  - All 78 JS functions present, all braces/parens/brackets balanced
  - FORECAST_LINEUP data properly embedded with both scenarios
  - Conference filter tabs on all views (Step 1, Step 2, Forecast, Forecast Lineup)
  - Scenario tabs functional for forecast view
  - No Python errors in any modified files

2026-03-07 - Frontend multi-team display consistency follow-up
---------------------------------------------------------------
- Updated `app/player_data_viewer.py` to aggregate per-player-season team context, merge canonical `player_team_stints.parquet` context, and surface it across card header, modal subtitle, and compare UI labels.
- Updated `app/player_bke_viewer.py` with embedded `STINT_TEAM_CONTEXT_MAP` (from `player_team_stints.parquet`) merged into client-side `TEAM_CONTEXT_MAP`, with team-column rendering that shows aggregated abbreviations plus stint suffix when applicable.
- Updated `app/player_eval_viewer.py` and `app/player_archetype_viewer.py` to merge canonical `player_team_stints.parquet` context into payload team labels.
- Fixed helper indentation/ordering issues in `app/player_eval_viewer.py` while applying the above.
- Regenerated all affected frontends:
	- `app/player_eval.html`
	- `app/player_archetypes.html`
	- `app/player_data.html`
	- `app/player_bke_viewer.html`
	- `app/simulation.html`
- Validation snapshot:
	- `get_errors` clean for updated viewer scripts.
	- `simulation.html` still contains scenario tabs/payload and `actual_wins` values.
	- Multi-team context now present in generated payloads:
		- `app/player_data.html`: `team_context_count > 1` for 111 entries (max 3)
		- `app/player_eval.html`: `team_context_count > 1` for 198 entries (max 3)
		- `app/player_archetypes.html`: `team_context_count > 1` for 111 entries (max 3)

2026-03-07 — Multi-Team Stint Architecture + Option B Scenario Forecasting
----------------------------------------------------------------------------
- Implemented stint derivation stage:
	- `src/features/derive_player_team_stints.py`
	- Outputs: `data/processed/player_team_stints.parquet`, `reports/player_team_stints_report.json`
- Implemented preseason roster snapshot fetcher (Option B source):
	- `src/data_fetch/fetch_preseason_rosters.py`
	- Outputs: `data/historical/preseason_rosters.parquet`, `data/historical/preseason_rosters/preseason_rosters_<season>.parquet`, `reports/preseason_rosters_report.json`
- Step 1 player-impact profiles are now stint-aware in `src/player_eval/build_player_impact_profiles.py`.
- Projection is now scenario-aware in `src/player_eval/project_next_season.py` via `--team-mapping-mode` (`end_of_season`, `preseason_snapshot`) with per-scenario output paths.
- Forecast pipeline now runs scenario-isolated steps and emits combined frontend payloads:
	- `src/simulation/run_forecast.py` orchestrates per scenario
	- scenario-specific artifacts: `projected_player_profiles_<scenario>.parquet`, `projected_team_features_<scenario>.parquet`, `forecast_*_<scenario>.json`
	- combined artifacts remain canonical for frontend (`forecast_season_results.json`, `forecast_validation.json`, `forecast_lineup_profiles.json`) with `default_scenario` + `scenarios` map.
- Frontend updated:
	- `app/simulation_viewer.py` supports scenario toggle/tabs.
	- `app/player_eval_viewer.py`, `app/player_data_viewer.py`, `app/player_archetype_viewer.py`, and `app/player_bke_viewer.py` now preserve stint context and avoid key collisions from multi-team seasons.
- Validation:
	- Modified scripts compile cleanly and viewers regenerate successfully.
	- `derive_player_team_stints.py` rerun successful (`rows=1580`, `multi_team_player_seasons=187`).
	- `run_forecast.py` scenario smoke runs successful for `end_of_season`, `preseason_snapshot`, and combined `end_of_season,preseason_snapshot` (with `--skip-lineup --skip-preseason-fetch`).
	- `fetch_preseason_rosters.py` import/dependency path is now unblocked with `nba_api==1.10.2`; in this environment, network calls to `stats.nba.com` can still be slow/intermittent and may require longer runtime due to request timeouts.
	- `app/simulation_viewer.py` updated to reconstruct scenario tabs from scenario-suffixed forecast artifacts when combined forecast JSON is legacy single-scenario.
	- `simulation.html` now includes East/West filter tabs (default `All Teams`) for both Step 1 and Forecast charts/tables.
- Team-rating regression degree experiment (season-holdout, 90 team-seasons):
	- MAE by polynomial degree: d1=1.9514, d2=1.9509, d3=1.9554, d4=1.9309 (best), d5=2.0867.
	- Degree-4 marginally improves MAE vs linear but with slightly worse RMSE.

- 2026-03-07 — Forecast Improvement Sprint + File Header Standardization
	- Critical bug fix: Mikal Bridges 8x duplication in defensive_archetypes_v2.parquet
		- Root cause: 8 identical rows for player_id 1628969 in 2022-23 defensive_archetypes_v2.parquet
		- The left merge in build_player_impact_profiles.py created a cartesian product (1 × 8 = 8 rows per player-season pair)
		- Fix: Added `.drop_duplicates(subset=["player_id", "season"], keep="first")` to defensive archetypes before merge
		- Impact: Profile count 1978→1971, BKN 2023-24 forecast improved from 59.0 to 49.0 wins (-10 error reduction)
	- Structural improvement 1: Availability discount in project_next_season.py
		- Role-based expected games: stars 88%, starters 92%, rotation 96%, bench 98%
		- Age discount: -1.5%/yr over 30 (max 10%); Prior-GP discount: -0.3%/game below 65 GP
		- ~40 new constants in src/player_eval/constants.py
	- Structural improvement 2: Replacement minutes buffer in project_next_season.py
		- Reserves 12% of team minutes for replacement-level contributors (BKE=-0.20)
		- Adds synthetic rows in projected profiles to represent bench depth
	- Structural improvement 3: Star concentration penalty in team_feature_aggregation.py
		- Measures top-1/top-2 player share of team BKE; penalizes top-heavy teams in forecast mode
		- Max penalty: 2.0 net rating points (fragility adjustment)
	- Validation: Backtest MAE=5.13/r=0.8861 UNCHANGED; Forecast 2023-24 margin MAE 8.20→7.52 (↓0.68), r 0.6711→0.6887 (↑0.018)
	- Multi-team roster proposal written at loop/proposal_multi_team_roster.md (AWAITING APPROVAL)
	- File header standardization: All 66 Python files in src/ + 5 app/ files now have unified format with path + separator lines
		- Fixed 2 wrong paths (team_feature_aggregation.py said src/player_eval/, train_minute_model.py said src/player_eval/)
		- Added headers to 3 files missing them entirely (build_player_impact_profiles, constants, player_name_normalizer)

- 2026-03-07 — Step 1 PPP quick-fix calibration pass completed and rerun
	- Root issue confirmed: PPP branch was mis-scaled vs margin path due profile-unit mapping and output display inflation (`ppp_net_rating` scale).
	- `src/simulation/season_sim.py` updates:
		- Added empirical season-level impact→net calibration (`_estimate_impact_to_net_scale`) and wired it into PPP game expectation build.
		- Kept offense/defense blend at `0.50`, added PPP clipping bounds, and possession-aware sigma exponent control (`possessions^0.50`).
		- Fixed displayed `ppp_net_rating` to use calibrated net units (removed erroneous `*100` inflation).
	- `src/simulation/simulation_config.py` updates:
		- Pace regression strengthened to `0.40` prior-team / `0.60` league mean.
		- Added centralized PPP calibration constants:
			- `PPP_IMPACT_SCALE_PER100`, `PPP_OFF_DEF_BLEND_WEIGHT`, `PPP_SIGMA_POSSESSION_EXPONENT`, `PPP_MIN`, `PPP_MAX`.
	- Rerun commands completed:
		- `python3 src/simulation/season_sim.py`
		- `python3 src/simulation/validate_sim.py`
		- `python3 src/simulation/run_forecast.py`
		- `python3 app/simulation_viewer.py`
	- Updated validation snapshot:
		- Step1 backtest overall (`simulation_step1_validation.json`):
			- `margin`: MAE `5.13`, RMSE `6.19`, corr `0.8861`
			- `ppp`: MAE `6.09`, RMSE `7.52`, corr `0.8815`
		- Forecast-style backtest seasons (`run_forecast.py` synthetic schedules):
			- 2023-24: `margin` MAE `8.20`, corr `0.6711`; `ppp` MAE `8.75`, corr `0.6305`
			- 2024-25: `margin` MAE `9.02`, corr `0.6375`; `ppp` MAE `8.46`, corr `0.6633`

- 2026-03-07 — Simulation Step 1 dual-model rollout (margin + PPP) completed
	- Implemented parallel Step 1 simulation outputs in `src/simulation/season_sim.py`:
		- `margin` model (existing net-rating path) retained as default.
		- `ppp` model added using minute-weighted team `impact_obke`/`impact_dbke` from player profiles.
		- Pace source is team-level prior-season pace from `team_game_logs.parquet` with regression-to-mean (`0.75` prior team, `0.25` prior league); no player-pace input.
		- Shared variance framework preserved: PPP game sigma scales existing margin sigma by possessions/100.
	- Output schema expanded for both backtest and forecast season results:
		- `season_stats_by_model`, `team_results_by_model`, `default_model`.
		- Team rows now include `model`, `ppp_offense`, `ppp_defense`, `ppp_net_rating`, `predicted_pace`.
		- Forecast mode now also attaches `actual_wins`, `actual_losses`, and `win_error` when historical records are available.
	- Updated validation in `src/simulation/validate_sim.py`:
		- Season-level metrics now reported in `overall_by_model`/`per_season_by_model` while keeping backward-compatible `overall`/`per_season`.
	- Updated frontend in `app/simulation_viewer.py`:
		- Added Step 1 and Forecast model toggles (`Margin` / `PPP`).
		- Tables/charts/stats now read model-specific payloads.
		- Forecast table and chart now show actual W-L and projection error when available.
	- Regenerated outputs:
		- `reports/simulation_step1_season_results.json`
		- `reports/simulation_step1_validation.json`
		- `reports/forecast_season_results.json`
		- `app/simulation.html`
	- Validation snapshot after rollout:
		- Step1 backtest overall (`simulation_step1_validation.json`):
			- `margin`: MAE `5.13`, RMSE `6.19`, corr `0.8861`
			- `ppp`: MAE `12.19`, RMSE `14.38`, corr `0.8499`
		- Forecast-style backtest seasons (`run_forecast.py` synthetic schedules):
			- 2023-24: `margin` MAE `8.20`, corr `0.6711`; `ppp` MAE `14.72`, corr `0.6123`
			- 2024-25: `margin` MAE `9.02`, corr `0.6375`; `ppp` MAE `16.13`, corr `0.6011`

- 2026-03-07 — Forecast v1.1 draft-aware rookie pass completed (fetch -> aggregate -> projection -> simulation)
	- Added new pipeline fetch stage: `src/data_fetch/fetch_player_draft_history.py`.
	- New canonical draft outputs: `data/historical/player_draft_history.parquet` and `data/historical/player_draft_history.csv`.
	- Added draft constants in `src/player_eval/constants.py` and merged draft fields into profile aggregate in `src/profile_aggregate/build_profile_aggregate.py`.
	- Replaced rookie CSV dependency as default path in `src/player_eval/project_next_season.py` with draft-aware rookie generation (CSV remains manual fallback).
	- Rookie priors shifted conservative and rookie scale tuning grid added (`[0.85, 1.00, 1.15]`), selected scale `0.85` by rookie BKE MAE.
	- Added conditional regression-to-mean for impact projection (stability/minutes-aware) and upgraded minutes projection using impact + salary + depth competition before 240-minute normalization.
	- Updated forecast orchestrator passthrough args in `src/simulation/run_forecast.py`: `--rookie-impact-scale`, `--no-rookie-scale-tune`.
	- Backtest-style forecast validation snapshot:
		- Wins: 2023-24 MAE `8.203`, r `0.6711`; 2024-25 MAE `9.017`, r `0.6375`
		- Margin proxy: 2023-24 MAE `4.404`, r `0.6760`; 2024-25 MAE `4.645`, r `0.5977`
		- Player carry: 2022-23->2023-24 BKE r `0.4194`, MPG r `0.7927`, rookie BKE MAE `0.2068`; 2023-24->2024-25 BKE r `0.4901`, MPG r `0.8189`, rookie BKE MAE `0.2153`
	- Documentation updated for this pass: `readme.md` and `reference/simulation/Simulation_Core_Summary.md` (append-only entry).

- 2026-03-06 — Forecast Pipeline Complete (Forward Projection from Backtesting)
	- Built full forecast pipeline: project_next_season.py → team_feature_aggregation(forecast) → season_sim(forecast) → lineup_projection(forecast)
	- Orchestrator: src/simulation/run_forecast.py with CLI (--forecast SEASON, --roster, --rookies, --skip-lineup)
	- New files: src/player_eval/project_next_season.py (~400 lines), src/simulation/run_forecast.py (~100 lines)
	- Modified: team_feature_aggregation.py, game_model.py (balanced schedule generator), season_sim.py, lineup_projection.py, simulation_config.py (forecast paths), constants.py (forecast constants), simulation_viewer.py (forecast tab)
	- Age curve empirically calibrated: breakpoints [21,24,27,30,33,36], BKE-scale deltas [+0.08,+0.04,+0.01,-0.01,-0.02,-0.02,-0.03]
	- Rookie tiers: lottery/mid_first/late_first/second_round/undrafted with preset BKE and MPG
	- Fixed NaN team bug in source profiles (89+202 players with null team_abbreviation filtered out)
	- Backtest validation: BKE r≈0.41-0.46, MPG r≈0.60-0.66
	- Forecast team-level: MAE≈9 wins, r≈0.65 (expected for preseason projection)
	- Frontend: Forecast tab with season tabs, stat cards, projected wins chart, sortable table, validation panel
	- No regression to original backtest pipeline (still MAE=6.1/3.8, r=0.867/0.951)
	- Outputs: data/processed/forecast/{projected_player_profiles.parquet, projected_team_features.parquet}, reports/{forecast_season_results.json, forecast_validation.json, forecast_lineup_profiles.json}

- 2026-03-06 — Simulation Step 2 final constraint tuning (starter/clutch eligibility + starter structure)
	- Added baseline lineup eligibility gates in `src/simulation/lineup_projection.py`:
		- projected starter/clutch candidates are ineligible when offensive OR defensive archetype is `Insufficient Minutes`.
		- projected starter lineups must include at least one canonical big (`position_band` in `Forward-Center` or `Center`).
		- added soft starter score penalty for sub-24 MPG candidates (`starter_low_minutes_threshold=24`, `starter_low_minutes_penalty=1.10`).
	- New Step 2 constants in `src/simulation/simulation_config.py` centralize these controls.
	- Canonical rerun + viewer refresh completed:
		- `python3 src/simulation/lineup_projection.py`
		- `python3 app/simulation_viewer.py`
	- Constraint audit checks on `reports/simulation_step2_lineup_profiles.json`:
		- insufficient-minutes archetype violations in projected starters/clutch: `0`
		- starter lineups missing true big (`Forward-Center`/`Center`): `0`
		- 2024-25 BKN Trendon Watford starter case: removed from projected starters.
	- Post-adjustment metrics (90 team-seasons):
		- starter_overlap_rate_mean: `0.7267`
		- clutch_overlap_rate_mean: `0.8667`
		- rotation_correlation: `0.6048`

- 2026-03-06 — Simulation Step 2 v1 optimization measures implemented + tuned defaults promoted
	- Implemented in `src/simulation/lineup_projection.py` with centralized knobs in `src/simulation/simulation_config.py`:
		- continuity prior (`continuity_factor = 1 - kappa * (1 - R)` from prior-season returning minutes)
		- clutch core constraint (candidate pool + minimum starter carryover + swap cap)
		- rotation regime model (`p_stagger = sigmoid(a*S - b*B)`) with regime-based rotation blend
	- Ablation/combination reports generated:
		- `reports/simulation_step2_v1_optimization_sweep.json`
		- `reports/simulation_step2_v1_regime_tuning.json`
		- `reports/simulation_step2_v1_combo_eval_tuned.json`
	- Final defaults promoted for canonical Step 2 runs:
		- `regime_star_coef=0.8`, `regime_bench_coef=0.6`
		- `rot_stagger_start/bench=0.65/0.35`, `rot_bench_start/bench=0.30/0.70`
		- continuity + clutch core + rotation regime toggles enabled
	- Canonical rerun completed:
		- `python3 src/simulation/lineup_projection.py`
		- `python3 app/simulation_viewer.py`
	- Post-promotion metrics (90 team-seasons):
		- starter_overlap_rate_mean: `0.6956`
		- clutch_overlap_rate_mean: `0.8644` (up from `0.8578`)
		- rotation_correlation: `0.5623` (up from `0.5526`)
		- observed-starter exact-5 coverage: `90/90`

- 2026-03-05 — Simulation Step 2 target-definition overhaul + exact-5 observed starters + simulator UX pass
	- Backend (`src/simulation/lineup_projection.py`) now validates starters from first-quarter PBP on-court lineups, with substitution-cluster verification, then season aggregation to observed starters.
	- Observed starter outputs are now guaranteed as exact five-player sets for every team-season in Step 2 payloads (`actual_starter_ids`, `actual_starter_names`).
	- Rotation benchmark updated to strict candidate filter: lineup rows with `total_poss >= 50` and starter-overlap `<= 2`, using average `NET_RTG` as observed target.
	- Clutch benchmark unchanged (top-5 clutch-minute players).
	- Frontend (`app/simulation_viewer.py`) single-game simulation upgraded with phase-edge decomposition, matchup-style context, and top-contributor summaries; layout refreshed for clearer hierarchy.
	- Regeneration/validation run:
		- `python3 src/simulation/lineup_projection.py`
		- `python3 app/simulation_viewer.py`
	- Post-change metrics (90 team-seasons):
		- starter_overlap_rate_mean: `0.6956`
		- starter_overlap_count_mean: `3.478`
		- clutch_overlap_rate_mean: `0.8578`
		- rotation_correlation: `0.5526`
		- observed-starter exact-5 coverage: `90/90`

- 2026-07-17 — Three-Task Sprint Complete: Availability Factor + Simulation Core + Frontend
	- Task 1 (PEC Step 4): Added availability_score to PlayerImpactProfile (games_ratio * age_factor, role-scaled). 1978 rows, mean=0.601, std=0.265. Aggregated as pec_availability_score.
	- Task 2 (Simulation Core Step 1): Built full 6-layer margin-based simulation. 3 scripts: game_model.py (params+game model+schedule), season_sim.py (Monte Carlo+aggregation), validate_sim.py (validation). Overall r=0.886, MAE=5.13, Brier=0.205-0.228, accuracy=65-69%. NAN team bug fixed. Simulation_Core_Summary.md fully populated.
	- Task 3 (Frontend): Created simulation_viewer.py → simulation.html. Season tabs, 8 stat cards, projected-vs-actual bar chart with CI bands, calibration plot, sortable 12-column table with color-coded errors. Dark theme, embedded JSON, fully standalone.

- 2026-03-04 — Reproduction pipeline stall remediation (Layer 2 observability + fail-fast)
	- Diagnosed "stuck" state as buffered logging + swallowed step failures in `scripts/reproduce_pipeline.sh`, not a deadlock.
	- Updated reproduction harness to run patched scripts with `python -u`/`PYTHONUNBUFFERED=1` for live logs.
	- Added fail-fast semantics: `run_patched()` now returns non-zero on step failure.
	- Added per-step runtime reporting (`Done (Ns)` / `FAILED (exit X, Ns)`).
	- Added detailed C5 progress logs in `src/modeling/model_rapm.py` around matrix build/fits so long RidgeCV stages are visible.
	- Layer 2 validation now shows forward progress with timings (C1 69s, C2 24s, C3 14s, C4 10s) and explicit C5 fit-stage logs.

- 2026-07-17 — PEC Step 3 architectural overhaul: scale decompression + talent dominance
	- Fixed scale compression: projected net rating std went from 0.64 to 5.07 (real NBA is ~5-6).
	- Talent backbone now dominates: 57%+61% of raw std (modifier/talent=7.5%, target <30%).
	- Rim protector detection expanded: includes Dropping Big + Mobile Big, teams_no_rp: 60% to 4.3%.
	- DEFAULT_TEAM_SCALE increased from 5 to 20 (BKE compression factor ~9x vs real RAPM).
	- All structure/defense penalty magnitudes reduced 50-70% via named constants.
	- Calibration skip logic added: skips OLS when holdout_r<0 or train_r<0.20 (prevents compression when data is unreliable).
	- Volatility model enhanced with roster depth component and wider alpha coefficients.
	- Per-season validation: 2022-23 r=0.19, 2023-24 r=0.49, 2024-25 r=-0.37 (game logs simulated).
	- Model correctly ranks BOS/OKC as elite, DET/POR/SAS as bottom across all seasons.
	- Files changed: constants.py, team_feature_aggregation.py. Viewer regenerated.

- 2026-03-03 — PEC v1 Step 3 remediation (ablation + calibration + volatility bounds)
	- Hard diagnostic pass added to `src/player_eval/team_feature_aggregation.py` after poor Step 3 signal: explicit ablation stack, train/holdout split, and calibration coefficients now emitted to Step 3 validation report.
	- Added defense sign inference (`off+def` vs `off-def` correlation check) and explicit reporting in JSON.
	- Corrected team-net construction scale: apply 5-man scaling to talent terms, keep interaction/structure/def-adjustments at team-level units.
	- Increased interaction influence by scaling pair weights (`(m_i*m_j)/0.2`) to avoid near-zero pair contributions.
	- Resolved volatility collapse: replaced hard [8,16] clipping with dynamic bounds tied to observed league net-rating std, so `vol_total` is no longer constant at 8.0.
	- Tightened spacing structure gate (efficiency + rotation-share requirement) to avoid inflated shooter counts.
	- Re-ran Step 3 and generated updated outputs/reports; correlation remains weak, but diagnostics now isolate where signal breaks (especially holdout instability).
	- Updated `readme.md` pipeline/data-layout and `reference/player_eval/Player_Evaluation_Core_Summary.md` Step 3 section to reflect remediation logic.

- 2026-03-05 — PEC v1 Step 3 + Salary/Behavioral Fixes + Team Aggregation Viewer
	- Salary pipeline fix: `normalize_name()` in fetcher now uses `unicodedata.normalize("NFKD", ...)` to properly decompose diacritics (Dončić→Doncic instead of Doni). Luka/Jokic salary data now correctly matched.
	- Removed salary from median-fill list (was creating $5M placeholders). Changed default to NaN. Frontend shows "Salary not found" for missing.
	- Fixed behavioral_three_point_rate and behavioral_free_throw_rate (were all 0.0 due to FGA/FG3A/FTA column name collision in merge — BKE decomp and stats both have these columns, causing _x/_y suffixes).
	- Added `stripDiacritics()` to frontend search so "doncic" matches "Dončić".
	- Implemented Step 3 Team Feature Aggregation (src/player_eval/team_feature_aggregation.py, 901 lines):
		- Offensive Mean Model: TalentBase + InteractionTerm (66-entry matrix) + StructureTerm (TOV, FTR, playmaking, spacing, transition as structure + volatility)
		- Defensive Mean Model: DefTalent + Essentials + AnchorQuality + Diversity - Liability
		- Volatility Model: base + 3PA + creation concentration + transition
		- Transition frequency appears in BOTH structure term (success-weighted) AND volatility model (freq deviation)
		- 92 team-seasons processed, validation vs actual net rating r=0.02 (calibration needed — expected for v1)
	- Added Team Aggregation tab to player_eval_viewer.py with team modal cards showing full offense/defense/volatility breakdowns + player roster + archetype interaction details.
	- All constants centralized in constants.py. PEC Summary updated.

- 2026-03-03 — PEC archetype consistency fix (aligned with player_data_viewer)
	- Root mismatch identified: PEC viewer selected top offense/defense from probability vectors; `app/player_data_viewer.py` uses canonical labels/confidence from archetype outputs.
	- Step 1 update in `src/player_eval/build_player_impact_profiles.py`:
		- Added canonical archetype fields from source files into Step 1 output (`off_primary_archetype`, `off_secondary_archetype`, `off_role_confidence`, `off_role_effectiveness`, `def_primary_archetype`, `def_secondary_archetype`, `def_role_confidence`).
		- Fixed defensive archetype merge collision via source-column renaming + coalescing.
	- PEC viewer update in `app/player_eval_viewer.py`:
		- Cards/modal now display canonical archetype labels + confidence fields from Step 1 (same semantics as player_data_viewer), while keeping distribution strips.
	- Rebuild completed: Step 1 (`player_impact_profiles.parquet`), Step 2 (`minute_model_predictions_v2.parquet`), and PEC HTML (`app/player_eval.html`).
	- Validation:
		- Archetype parity against source parquet is 100% when source rows are present; overall ~85.7% due ~291 non-eligible rows without source archetype rows.
		- Minute-model audit for leakage review: `corr(pec_defensive_shrinkage_lambda, MPG)=0.219`; ablations run (no-lambda, no-salary, no-lag_mpg) and logged in session output.

- 2026-03-03 — PEC v1 Step 1/2 implementation completed (deterministic profile + minute model)
	- Added new Player Evaluation core scripts under `src/player_eval/`:
		- `constants.py` — centralized PEC input/output paths and modeling constants
		- `build_player_impact_profiles.py` — Step 1 player impact object construction from RAPM/BKE/archetype/diagnostic artifacts
		- `train_minute_model.py` — Step 2 deterministic minute-share model with season-grouped CV and team-share normalization constraints
	- Step 1 outputs generated:
		- `data/processed/player_eval/player_impact_profiles.parquet`
		- `data/processed/player_eval/player_profiles_season.pkl`
		- `reports/player_eval_step1_validation.json`
	- Step 2 outputs generated:
		- `data/processed/player_eval/minute_model_v1.pkl`
		- `data/processed/player_eval/minute_model_predictions_v1.parquet`
		- `reports/player_eval_step2_minute_model_validation.json`
	- Validation highlights:
		- Step 1 rows: 1978 across 2022-23/2023-24/2024-25
		- Step 1 archetype probability sums are numerically normalized (mean = 1.0 for offense/defense vectors)
		- Step 2 grouped CV MAE: 0.0119 / 0.0060 / 0.0009 by held-out season
		- Step 2 holdout (2024-25): MAE 0.0119, R² 0.6155, team Spearman 0.9978
		- Step 2 team-share simplex constraints hold exactly (team_sum_mean=1.0)
	- Runtime note:
		- `xgboost` not available in execution environment at run time; Step 2 now includes deterministic fallback to `sklearn.ensemble.GradientBoostingRegressor` so pipeline remains runnable.
	- Documentation updates:
		- `readme.md` updated with PEC step order and new output/report artifacts.

- 2026-03-01 — Interactive BKE viewer + model_config centralization
	- Added interactive BKE viewer pipeline:
		- `scripts/export_bke_components.py` — exports per-player BKE components to `data/processed/bke/bke_v31_components.json`
		- `app/player_bke_viewer.py` — generates standalone `app/player_bke_viewer.html` with:
			- Lambda slider (0.00–0.15) for real-time production tilt adjustment
			- O/D split toggle (60/40 vs 55/45)
			- Sortable table with rank, BKE, OBKE, DBKE, production proxy, RAPM
			- Season filter, player search, Top-N bar chart, rank-shift distribution
			- All computation happens client-side from 950-player JSON
		- Formula: `bke_final = bke_base + lambda * prod_proxy_z`
		- Parity verified at lambda=0.03 vs experiment2 report: exact match on all metrics
	- Centralized v3.1 tuning knobs into `src/modeling/model_config.py`:
		- Added `V31ExperimentalConfig` dataclass and `V31_EXPERIMENTAL` singleton
		- Centralizes: OBKE/DBKE sub-weights, O/D split options, L3/L6 defaults, production proxy weights, cohort thresholds, sweep grids, defensive driver share guardrails
		- Updated all consumers to import from model_config instead of hardcoding:
			- `src/modeling/bke_v31_experimental_layers.py`
			- `src/modeling/experiment2_production_tilt.py`
			- `scripts/export_bke_components.py`
		- Post-centralization audit: 0 hardcoded BKE weights remain in computation code
	- Added `BKE_V31_COMPONENTS_JSON` path constant to model_config
	- Updated `readme.md` with new viewer/exporter commands and data layout

- 2026-03-01 — Experiment 2 rerun (expanded production proxy, 15 lambdas) + next-phase prep
	- Added reproducible script: `src/modeling/experiment2_production_tilt.py`
		- output: `reports/bke_v31_experiment2_production_tilt.json`
		- base profile: Layer3+Layer6 (`layer3_exponent=1.08`, `layer6_k=0.20`, `60/40`)
		- expanded production proxy includes ORAPM + TS_PCT + raw offensive box score stats (`PTS`, `AST`, `FGM`, `FGA`, `FG3M`, `FG3A`, `FTM`, `FTA`)
		- 15-lambda sweep (`0.01`..`0.15`) completed
		- recommended small-change lambda: `0.03` (predictive rho improvement with controlled rank shift)
	- Added helper scripts:
		- `scripts/run_experiment2_production_tilt.sh`
		- `scripts/backup_data_snapshot.sh`
	- Added next phase scaffold:
		- `src/player_eval/README.md`
	- Reorganized references into contribution-based subfolders under `reference/` and added:
		- `reference/README.md`
		- `reference/BKE_gen1_summary_2026-03-01.md`
	- Updated docs/scripts for path and pipeline alignment:
		- `readme.md`
		- `scripts/compute_data.sh` (fixed archetype viewer path)
		- `scripts/compare_data_batches.py` (reference path comment)

- 2026-02-28 — v3.1 dual split policy + Layer 3+6 second-pass promotion
	- Updated `src/modeling/bke_v31_experimental_layers.py` to co-produce v3.1 split outputs:
		- `data/processed/bke/BKE_Scores_v31_60_40.json`
		- `data/processed/bke/BKE_Scores_v31_55_45.json`
	- Added dedicated second-pass evaluation (Layer 3 + Layer 6 only, no Layer 1 weight shift):
		- report: `reports/bke_v31_layer36_second_pass.json`
		- additive gate result: **PASS**
		- promoted outputs:
			- `data/processed/bke/BKE_Scores_v31_60_40_layer36.json`
			- `data/processed/bke/BKE_Scores_v31_55_45_layer36.json`
	- Updated player viewer to support split toggle (`60/40`, `55/45`) alongside version toggle:
		- file: `app/player_data_viewer.py`
		- regenerated: `app/player_data.html`
	- Updated docs for persistence across future sessions:
		- `readme.md`
		- `reference/BKE_metric_modelling_summary.md`
		- `reference/BKE_metric_modelling_plan.md`

- 2026-02-28 — Phase 2 v3.1 experimental layer framework implemented
	- Added new experimental runner: `src/modeling/bke_v31_experimental_layers.py`.
	- Runner behavior:
		- Loads `data/processed/bke/bke_v28_decomposition.parquet` (qualified only).
		- Computes fixed baseline metrics and evaluates each v3.1 layer independently:
			1) O/D weight grid
			2) Specialty-aware dampening
			3) Defensive tail micro-convex scaling
			4) Confidence-weighted defensive blend
			5) Defensive dimensional cleanup (orthogonal PCA confidence probe)
			6) Historical axis volatility scaling
		- Exports full layer-by-layer report to `reports/bke_v31_experimental_layers.json`.
		- Applies constrained combined-model selection (max 3 structural changes, guardrails to block unstable candidates).
	- Phase 2 run completed successfully (`generated_at: 2026-02-28 21:59:31`, 950 qualified players).
	- Combined model selected by guardrails:
		- `layer1_weight_grid` + `layer6_axis_volatility_scaling` + `layer3_def_tail_scaling`
		- Layer 4 was tested independently but excluded from combined model due instability/retention guardrail failures.
	- Documentation updates:
		- `readme.md`: added v3.1 experiment runner command + report artifact entry.
		- `reference/BKE_metric_modelling_summary.md`: appended full v3.1 experimental section with independent layer results and controlled combined evaluation.

- 2026-02-28 — Phase 1 balanced defensive tuning + BKE output rerun
	- Updated `src/data_compute/compute_defensive_archetypes_v2.py` to balanced policy:
		- Margin fallback gate: `MARGIN_RULE_GAP = 0.03` (from 0.05).
		- Confidence formula: `0.50*top + 0.50*(0.5 + 0.5*margin^0.4) + 0.03*max(0, top-0.75)`.
	- Fixed BKE decomposition blocker in `src/modeling/model_config.py`:
		- Added missing `PortableTalentConfig` defensive phase-b fields used by Layer 1 (`enable_defensive_phase_b`, archetype/global blend weights, convex/scale/clip params).
		- Defaulted `enable_defensive_phase_b=False` for backward compatibility.
	- Phase 1 reruns completed:
		1) `python3 src/data_compute/compute_player_archetypes.py`
		2) `python3 src/data_compute/compute_defensive_archetypes_v2.py`
		3) `python3 src/modeling/decomposition_engine.py`
		4) `python3 src/modeling/construct_bke_scores_v27.py`
		5) `python3 src/modeling/dbke_v30_defense_shrinkage.py` (phase_c remains FAIL gate, expected by script)
		6) `python3 app/player_data_viewer.py`
	- Output refresh confirmed: `player_archetypes.parquet`, `defensive_archetypes_v2.parquet`, `bke_v28_decomposition.parquet`, `BKE_Scores_v27.json`, `dbke_v30_defense_shrinkage.json`, `app/player_data.html`.

- 2026-02-28 — Defensive archetype confidence EXP D applied + downstream rerun
	- Updated `src/data_compute/compute_defensive_archetypes_v2.py` confidence formula to EXP D:
		- `confidence = 0.40 * top_role_score + 0.60 * (0.5 + 0.5 * role_margin^0.6)` with [0,1] clipping on inputs.
	- Fixed file integrity issues in same script introduced by prior edit:
		- `load_tracking_defense()` now correctly reads parquet before setting season.
		- `load_speed_distance()` restored valid control flow (removed stray triple-quote break).
	- Rerun from defensive stage onward (all successful):
		1) `python3 src/data_compute/compute_defensive_archetypes_v2.py`
		2) `python3 app/player_archetype_viewer.py`
		3) `python3 app/player_data_viewer.py`
		4) `python3 src/utils/export_db_to_parquet.py`
	- Output check: `defensive_confidence` summary now reports mean ~0.74 in script diagnostics; viewers regenerated.

- 2026-02-27 — BKE v3.0 Standard Applied Across Repo + Viewer Enhancements
	- **Model change: v27 60/40 blend**
		- `src/modeling/construct_bke_scores_v27.py` line 286: `raw_BKE = raw_OBKE + raw_DBKE` → `raw_BKE = 0.60 * raw_OBKE + 0.40 * raw_DBKE`
		- Matches v3.0's offense-biased blend (offense_delta=0.10)
		- Method doc updated: `"BKE_raw": "0.60*OBKE_raw + 0.40*DBKE_raw  [v3.0 60/40 offense/defense blend]"`
		- v27 JSON regenerated (950 players, verified: SGA BKE=1.0335 = 0.60*1.3766 + 0.40*0.5189)
	- **Viewer: O/D Lean Indicator**
		- Cards now show a 44px split bar (red=offense, teal=defense) below stats row
		- Width proportional to OBKE%/(OBKE%+DBKE%), title tooltip shows exact percentiles
		- Requires `obke_pct` and `dbke_pct` in each BKE version's card data
		- v28 loader enhanced to compute per-season OBKE/DBKE rank percentiles
	- **Viewer: Season Navigation Arrows**
		- Left/right chevron buttons outside modal card (absolutely positioned in overlay)
		- Finds all card keys for same player_id, sorts by season, navigates prev/next
		- Disabled at boundaries (first/last season)
	- **Viewer: Two-Player Comparison**
		- "Compare" button in controls bar opens full comparison overlay
		- Two search inputs with autocomplete dropdown (first 20 matches, shows name+season+team)
		- Head-to-head stats grid: PPG, APG, RPG, MPG, USG%, TS%, BKE, OBKE, DBKE, Off Fit, Off Eff, Def Fit
		- Better value highlighted in teal; archetype rows shown without highlighting
		- SVG radar chart with 8 normalized dimensions (PPG, APG, RPG, USG%, TS%, BKE, Off Fit, Def Fit)
		- Concentric reference circles (25/50/75/100%), axis labels, two overlapping semi-transparent polygons
		- Legend shows player colors (red/teal)
	- HTML regenerated: 11.6MB, 953 cards

- 2026-02-27 — BKE Version Toggle in Player Data Viewer
	- Added seamless BKE version switching (v27 / v28 / v3.0) to `app/player_data_viewer.py`:
		- New loaders: `load_bke_v28_scores()` (obke_dbke, layers, dimensions), `load_bke_v30_scores()` (defense shrinkage)
		- Cards embed `bke_versions` dict with per-version pct/rank/bke/obke/dbke
		- Details store includes `bke_v28` and `bke_v30` sections (layer z-scores, dimension z-scores, defense pipeline fields)
		- v28: composite BKE = 60% obke_transformed + 40% dbke_transformed, ranked per season
		- v30: percentiles computed from raw BKE_v30 / DBKE_scaled_v30 / OBKE_v30_reference per season
	- Frontend changes:
		- Version toggle buttons (v27 / v28 / v3.0) with orange active indicator in controls bar
		- `getBkeField()` helper routes card BKE display through active version
		- Sort by tBKE / tOBKE / tDBKE uses active version values
		- Modal BKE Highlights renders version-specific fields (v27 percentiles, v28 decomposition, v30 defense shrinkage)
		- Modal BKE Details KV shows version-specific raw data
		- Reset button restores version to v27
	- Payload impact: 10.2MB → 11.6MB (+14%), 953 cards, 947 with all 3 versions
	- All v27 behavior preserved as default; no production data changed

- 2026-02-22 — BKE v3.0 Phase B Structural Rewrite (Stable Geometry)
	- Replaced prior Phase B path in `src/modeling/dbke_v30_defense_shrinkage.py`:
		- removed hard archetype scale clipping and tanh tail saturation step
		- added global standardization (`DBKE_norm_v30 = DBKE_raw_v30 / sigma_global`)
		- added smooth negative-tail compression in z-space (`z/(1+gamma*|z|)` for z<0)
		- added hierarchical within-archetype mean shrinkage (`eta*z + (1-eta)*mu_arch`)
		- rescaled to raw defensive scale for `DBKE_final_v30`
	- Rerun result (`reports/dbke_v30_defense_shrinkage.json`):
		- Phase A: PASS
		- Phase B: PASS
			- `penalty_asymmetry_pre=0.339616` -> `post=0.252545` (improved)
			- `archetype_spread_pre=0.102975` -> `post=0.089529` (improved)
		- Phase C: FAIL (`status=stopped_after_phase_c`)
			- failing gate: `def_specialist_dbke_yoy_corr=0.412499` (< min 0.45)
	- `readme.md` modeling step note updated to describe new Phase B structure.

- 2026-02-22 — BKE v3.0 Phase C Reliability-Weighted Scaling
	- Updated Phase C in `src/modeling/dbke_v30_defense_shrinkage.py` to replace uniform DBKE scaling with reliability-aware scaling:
		- old: `DBKE_scaled = DBKE_final * scale_applied`
		- new: `DBKE_scaled_i = mu + scale_applied * lambda_shrink_i * (DBKE_final_i - mu)`
	- Validation rerun results (with `prior_blend_w=0.70`):
		- `def_specialist_dbke_yoy_corr = 0.420951` (improved vs pre-change 0.412499, still below gate 0.45)
		- `dbke_yoy_corr = 0.709442`
		- `def_driver_share = 0.363125`
		- status remains `stopped_after_phase_c`
	- Tested fallback suggestion `prior_blend_w: 0.70 -> 0.75`; outcome worsened specialist stability (`0.383258`) and was reverted to `0.70`.
	- `readme.md` updated to reflect Phase C reliability-weighted scaling behavior.

- 2026-02-22 — Added missing specialist rank-stability diagnostics (Phase C)
	- Extended `src/modeling/dbke_v30_defense_shrinkage.py` Phase C metrics to include:
		- Spearman rank YoY for specialists
		- Top-10% retention YoY
		- Top-20% retention YoY
	- Added both specialist definitions:
		1) top-25% by global mean `D_port`
		2) top-25% by baseline-season `DBKE_scaled_v30` (pairwise)
	- Latest run (`status=stopped_after_phase_c`) diagnostics:
		- Definition 1 avg: Spearman `0.322051`, Top10 retention `0.5625`, Top20 retention `0.433334`
		- Definition 2 avg: Spearman `0.379722`, Top10 retention `0.571429`, Top20 retention `0.428571`
	- Interpretation from run: baseline-DBKE specialist definition improves rank stability vs D_port-defined specialist cohort while retention is broadly similar.

- 2026-02-21 — BKE v3.0 Defense Reconstruction (Phase-Gated Script)
	- Added `src/modeling/dbke_v30_defense_shrinkage.py` implementing plan phases:
		- **Phase A:** DRAPM shrinkage to portable prior + 3-year smoothing + DBKE_raw reconstruction
		- **Phase B:** within-archetype variance normalization + negative-tail compression + tanh geometry shaping
		- **Phase C:** variance equalization check + offense/defense rebalance into final `BKE_v30`
	- Added output path constant in `src/modeling/model_config.py`:
		- `BKE_V30_DEFENSE_SHRINKAGE_JSON = reports/dbke_v30_defense_shrinkage.json`
	- Validation behavior implemented per request:
		- Gate checks run at end of each phase.
		- If a phase fails, script stops immediately and writes single output JSON.
	- First run result:
		- Phase A: PASS (`corr_DBKEraw_DRAPM=0.856`)
		- Phase B: FAIL (tail/asymmetry + archetype spread checks)
		- Iteration stopped at Phase B (`status=stopped_after_phase_b`)
	- Output artifact:
		- `reports/dbke_v30_defense_shrinkage.json`
	- Updated `readme.md` modeling step and reports layout to include v3.0 script/output.

- 2026-02-21 — BKE v2.9 Diagnostic Suite + Pipeline Reorganization + Viewer Updates
	- **Task 1 (Adjustment Script):** Rewired `scripts/BKE_model_config_adjustment.sh` verification section for new directory structure. Pipeline chain: model_rapm → ingest_darko → decomposition_engine → construct_bke_scores_v27 → player_data_viewer.
	- **Task 2 (BKE Rank Display):** Added ordinal suffix helper (`ordSuffix`) and `fbkeCard(pct,rank)` JS function in `app/player_data_viewer.py`. Card now shows `BKE: 98.9% (11th)` format.
	- **Task 3 (Directory Reorganization):** Created `data/processed/bke/` and `reports/` directories. Moved BKE decomposition files to `data/processed/bke/`, report files to `reports/`. Updated all path references in: `model_config.py`, `backtesting.py`, `ingest_darko.py`, `decomposition_engine.py`, `construct_bke_scores_v27.py`, `player_data_viewer.py`, `BKE_model_config_adjustment.sh`, `compare_bref_advanced_metrics.py`. Full audit completed.
	- **Task 4 (v2.9 Diagnostic Suite):** Created `tests/bke_v29_diagnostics.py` — 8-domain diagnostic suite:
		1. OBKE/DBKE Variance Asymmetry Audit (tests 1.1-1.3)
		2. Defensive Signal Quality Audit (tests 2.1-2.3)
		3. Counting Stats vs On/Off Dominance (tests 3.1-3.3)
		4. Variance Anchor Stress Test (tests 4.1-4.2)
		5. Offense Weight Bias Experiment (test 5.1)
		6. Archetype Coefficient Audit (tests 6.1-6.3)
		7. Portability vs Role-Dependent Drag (test 7.1)
		8. Rank Movement Driver Decomposition (test 8)
	- Output: `reports/bke_v29_diagnostic_master.json` (525KB, 950 players, 3 seasons, 0.15s runtime)
	- Key findings: Variance symmetric (48/52), DBKE-DRAPM corr=0.87, no counting stat dilution, balanced rank drivers, no anchor leakage
	- Updated: `model_config.py` (added `BKE_V29_DIAGNOSTIC_JSON`), `readme.md` (pipeline + data layout), `reference/BKE_metric_modelling_summary.md` (v2.9 section appended)
	- No structural/weight changes — measurement only iteration.

- 2026-02-21 — BKE v2.8 completion + card stat-slot update
	- Updated `app/player_data_viewer.py` card overview row to display `BKE` percentile in the former `RIM` slot and removed external card-level BKE badge usage.
	- Implemented v2.8 decomposition integrity updates in `src/modeling/model_config.py` + `src/modeling/decomposition_engine.py`:
		- Seasonwise variance anchoring for `role_dependent_impact_z` and `total_impact_z`
		- Logistic terminal transform before percentile ranking for portable/role-dependent/total scores
		- Added distribution diagnostics and expanded decomposition artifacts
	- Full decomposition run validated successfully (`~62.8s`, `950` qualified players), saving:
		- `bke_v28_decomposition.parquet/.csv`, `bke_v28_report.json`
		- `bke_v28_variance_report.json`, `bke_v28_compression_report.json`
		- `dimension_scores_v28.json`, `layer_scores_v28.json`, `obke_dbke_scores_v28.json`
	- Updated docs to reflect v2.8 decomposition outputs while retaining v27 final constructor artifact path (`BKE_Scores_v27.json`).

- 2026-02-21 — BKE viewer UX + grouped percentile enrichment
	- Updated `app/player_data_viewer.py` cards with a compact `BKE #rank · percentile` badge.
	- Sort controls updated to `Off Fit`, `Off Effectiveness`, plus transformed metric sorts: `tBKE`, `tOBKE`, `tDBKE`.
	- Improved modal visual hierarchy (stronger section cards, borders, and contrast for headers/dividers/content blocks).
	- Extended `BKE Highlights` to show grouped percentile context (position band, offensive archetype, defensive archetype) for BKE/OBKE/DBKE.
	- Extended `src/modeling/construct_bke_scores_v27.py` to compute grouped transformed percentiles using existing decomposition columns: `position_bucket`, `primary_archetype`, `defensive_archetype`.
	- Regenerated outputs: `data/processed/BKE_Scores_v27.json`, `app/player_data.html`.

- 2026-02-20 — BKE scores name resolution + viewer BKE sections
	- Fixed five `Unknown` names in `data/processed/BKE_Scores_v27.json` by adding season-aware/player-id fallback lookup in `src/modeling/construct_bke_scores_v27.py`.
	- Authoritative name fallbacks sourced from processed/historical parquet files (`player_archetypes`, `archetype_embeddings`, `defensive_archetypes_v2`, `complete_player_season_stats`).
	- Updated `app/player_data_viewer.py` to load `BKE_Scores_v27.json` and render:
		- `BKE Highlights` section in modal (between Season Highlights and Archetypes)
		- `BKE Details` section at modal bottom under `Linear Metrics (BKE-computed)`.
	- Regenerated artifacts: `data/processed/BKE_Scores_v27.json`, `app/player_data.html`.

- 2026-02-20 — Final Comprehensive BKE output constructor added (v2.7)
	- Added `src/modeling/construct_bke_scores_v27.py` to build terminal OBKE/DBKE/BKE outputs from raw/z layer composites only.
	- Added centralized output path constant `BKE_SCORES_V27_JSON` in `src/modeling/model_config.py`.
	- New artifact: `data/processed/BKE_Scores_v27.json` (eligible players only; final ranking by terminal league-wide `final_BKE_percentile`).
	- Updated `readme.md` pipeline order and data layout, and appended final-construction addendum to `reference/BKE_metric_modelling_summary.md`.

- 2026-02-20 — BKE v2.7 Statistical Maturity Pass Implemented
	- Implemented v2.7 fixes in modeling stack (`src/modeling/*`):
		- Soft archetype memberships from embeddings (distance→softmax with floor), producing `arch_prob_*` + entropy columns.
		- Full conditional neutralization (mean + variance conditioning) integrated in `percentile_engine.py` and applied in Layer 1.
		- Empirical-Bayes variance-component shrinkage path added in shared shrinkage utility and wired into defensive playmaking shrinkage.
		- RUE optimal-source decoupled from RAPM via config (`rue_optimal_source`) and soft-membership weighted optimal vectors added.
		- Layer 3 elevation baselines upgraded to support soft-membership weighted expectations.
		- Portability transfer tightened with soft-membership entropy signal.
		- Dimension model variance restoration added (target std default 0.42).
	- Version/output naming moved to v2.7/v27:
		- `bke_v27_decomposition.parquet`, `bke_v27_decomposition.csv`, `bke_v27_report.json`, `bke_v27_backtest.json`.
	- Updated docs:
		- `reference/BKE_metric_modelling_summary.md` rewritten for v2.7 behavior and math.
		- `readme.md` pipeline/data-layout modeling outputs updated to v27 names.

- 2026-07-17 — BKE v2.6 Session: Bayesian Shrinkage Fix + L4 Fix + Backtesting + System Audit
  - Fixed Bayesian shrinkage calibration in `src/modeling/bayesian_hierarchical.py`:
    - Bug: player_noise_var = within_var / reliability gave λ=0.5 for ALL players (50% shrinkage)
    - Fix: player_noise_var = within_var / (GP * reliability), so full-season players get ~1% shrinkage
    - Result: Dimension stds preserved (shooting 0.522→0.493, only 5.5% reduction)
  - Fixed L4 (Scheme Stability) penalizing stars in `src/modeling/decomposition_engine.py`:
    - Bug: Negative scheme_z (SGA=-2.299, Giannis=-2.030) subtracted from TI
    - Fix: Clipped scheme_z to >=0 in TI formula (bonus-only, no penalty)
    - Result: SGA 100.0 (#1), Giannis 99.7 (#2), Jokic 99.1 (#4) in 2024-25
  - Backtesting framework (`src/modeling/backtesting.py`) created and run:
    - PTS Spearman ρ=+0.553, TI within-1-tier accuracy=76.0% (n=263 returning)
    - Dimension stability: driving (0.840) most stable, playmaking creation (0.297) least
    - Archetype stability: 53.6% (offensive + defensive)
	- Full system audit written to `loop/system_audit_v2.6.md`:
    - Overall grade: B- (good framework, role player inflation needs fixing)
    - Top issue: Layer 3 elevation inflates role players (Gafford #3, Pritchard #5)
    - All 3 deferred features now implemented and validated
  - Pipeline: 950 qualified, ~12s, 344 columns, 3 seasons

- 2026-02-20 — BKE v2.5 Implementation Complete (upgraded from v2.0)
- Updated 5 modules in `src/modeling/` to v2.5:
  - `model_config.py` — Added NeutralizationConfig (min_cohort=8, shrinkage=0.50, 6/8 dims neutralized), TRACKING_DIR, v2.5 output paths (bke_v25_*), expanded PORTABLE_DIMENSIONS: playmaking→playmaking_creation with creation+pressure sub-components, turnover_control adds TOV_PER_TOUCH/DRIVE_TOV_RATE, defensive_playmaking adds CHARGES_DRAWN/DEF_LOOSE_BALLS_RECOVERED
  - `percentile_engine.py` — Added neutralize_by_archetype() and neutralize_dimensions() functions for archetype-conditional neutralization with small-cohort shrinkage
  - `layer1_portable_talent.py` — Added load_hustle_stats(), compute_derived_metrics(), rewrote compute_dimension_model() with playmaking creation+pressure split (MF-5), enhanced TO metrics (MF-1), expanded def playmaking (MF-4), archetype neutralization call, three-level z-scores (positional + archetype)
  - `layer3_archetype_elevation.py` — Fixed elevation_z bug (was being dropped by cleanup code)
  - `decomposition_engine.py` — Updated to v2.5: version refs, portability_ratio removed from public outputs (Fix #4), portability_index used in report/card/console prints
- Results: 950 qualified player-seasons across 3 seasons, 0 NaN, 314 columns
- Outputs: bke_v25_decomposition.parquet (314 cols, 1971 rows), .csv, bke_v25_report.json
- Key v2.5 validation:
  - 6 neutralization raw columns present (dim 1-4, def playmaking, TO control)
  - Neutralized dim stds slightly lower than v2.0 raw (expected: archetype variance removed)
  - 11 positional z-score columns, 14 archetype z-score columns
  - Playmaking creation sub z and pressure sub z stored for diagnostics
  - TOV_PER_TOUCH, DRIVE_TOV_RATE, DRIVE_AST_RATIO, PASSES_MADE_PER36 derived and active
  - CHARGES_DRAWN, DEF_LOOSE_BALLS_RECOVERED loaded from hustle_stats.parquet (1669 rows merged)
  - Portability class: 286 Scalable Star, 285 Context-Moderate, 379 System Player
  - Runtime: ~8.3s
- No new data fetcher needed — all data already existed
- Updated reference/BKE_metric_modelling_summary.md to v2.5 with full changelog
- Known issues deferred: Fix #5 (archetype instability → v2.6), MF-3 (confidence intervals), stability-weighted dims, backtesting, hierarchical regression, missing tracking metrics

- 2026-07-16 — BKE v2.0 Implementation Complete (upgraded from v1.5)
- Updated all 7 modules in `src/modeling/` to v2.0:
- `model_config.py` — Added ZScoreConfig, BayesianShrinkageConfig, PortabilityConfig, 8-dimension PORTABLE_DIMENSIONS dict, v2.0 output paths, 25/20/55 Layer 1 weights
- `percentile_engine.py` — Added compute_z_score(), add_league_z_scores(), add_grouped_z_scores(), z_to_percentile(), weighted_z_composite(), apply_bayesian_shrinkage()
- `layer1_portable_talent.py` — Rewrote to 8-dimension model with z-score aggregation: Shooting Gravity, Driving Gravity (revised), Playmaking, Extra Possession (cross-domain), Defensive Playmaking (expanded), Defensive Impact, Turnover Control (standalone), Defensive Versatility. Added _avg_z_components() for NaN-safe averaging. Expanded data loaders to include tracking columns.
- `layer2_role_utilization.py` — Added z-score outputs for surplus and RUE. Fixed min_playtype_poss from 25 (season total) to 0.3 (per-game) — surplus was always zero in v1.5.
- `layer3_archetype_elevation.py` — Added elevation_z output
- `layer4_scheme_amplification.py` — Added scheme_stability_z output
- `decomposition_engine.py` — Rewrote compute_total_impact() to use z-score aggregation, replaced compute_portability_ratio() with compute_portability_index() (4-component variance-based), updated generate_player_card/report/run_full_decomposition to v2.0
- Results: 950 qualified player-seasons across 3 seasons, 0 NaN in all dimension/impact/portability columns
- Outputs: bke_v20_decomposition.parquet (~280+ cols), .csv, bke_v20_report.json
- Key v2.0 metrics validated:
  - All 8 dimension z-scores have 0 NaN and meaningful variance (std 0.277-0.892)
  - Portable talent z: mean ~0.0, std 0.478
  - Total impact z: mean ~0.0, std 0.455
  - Portability index raw: mean 0.605, std 0.078
  - Surplus now active (abs mean 0.325, was 0.0 in v1.5)
  - Role elasticity has variance (std 0.083, was 0.0 before fix)
- Bugs fixed during v2.0 implementation:
  - assignment_difficulty was categorical string ("Low"/"Medium"/"High") — converted to numeric 0/0.5/1.0
  - Offensive archetypes loader missing driving/tracking/entropy columns — expanded keep list
  - Defensive archetypes loader missing hustle/engagement/deflection/matchup columns — expanded keep list
  - min_playtype_poss=25 never matched per-game data (max ~10/game) — changed to 0.3
  - z-component NaN propagated through dimension averages — added fillna(0) in _avg_z_components()
  - emb_entropy_norm used as proxy for pcv_entropy (not computed)
- Updated reference/BKE_metric_modelling_summary.md to v2.0 with all changes from v1.5 and key differences from v2.0 plan


- 2026-02-16 — RAPM refactor + DARKO pipeline scaffolding
	- Added new RAPM script: `src/modeling/model_rapm.py` and removed legacy modeling scripts:
		- `src/modeling/compute_rapm.py`
		- `src/modeling/compute_xrapm.py`
		- `src/modeling/compute_xrapm_improved.py`
	- RAPM fixes implemented:
		- ORAPM/DRAPM now estimated with opponent controls via joint split matrix (offense + defense coefficients in one model).
		- Removed zero-fill schema issue by explicitly carrying `RAPM` on split rows as `ORAPM + DRAPM`.
		- Default possession weighting switched to uniform (`POSSESSION_WEIGHT_MODE = "uniform"`), with optional duration weighting retained.
		- Alpha grids adjusted to less aggressive ranges.
		- Multi-season output now uses pooled-prior then target-season refinement to reduce season-blur from one pooled coefficient vector.
		- In-sample `R²` output relabeled as diagnostic only.
	- DARKO pipeline added:
		- `src/data_fetch/fetch_darko_manual.py` (manual CSV staging)
		- `src/data_normalize/normalize_darko.py` (canonical DARKO normalization)
		- `src/modeling/ingest_darko.py` (builds `modeling_inputs_all` and `modeling_inputs_{season}` outputs)
	- Documentation updates:
		- New plain-language model explainer: `reference/modeling_info.md`
		- Pipeline/docs/scripts updated to `model_rapm.py` + DARKO normalize/ingest flow (`readme.md`, `scripts/compute_data.sh`, `scripts/reproduce_pipeline.sh`, tests + references).

- 2026-02-16 — Post-move RAPM audit and validation
	- Ran `python3 tests/validate_rapm.py` after modeling-path migration; suite completed successfully with stable pooled RAPM distribution and season-to-season stability checks passing.
	- Path audit confirms active pipeline/docs now point to `src/modeling/` for RAPM/xRAPM scripts; remaining legacy references are limited to historical DO_NOT_CHANGE planning text.
	- Follow-up analytical finding: split-type rows (`pooled_split`, `single_season_split`) carry ORAPM/DRAPM fields but no RAPM scalar, which can surface as zero-variance if validators consume the RAPM column blindly.

- 2026-02-15 — Modeling layer restructure + advanced modeling plan kickoff
	- RAPM/xRAPM scripts moved from `src/data_compute/` to `src/modeling/`:
		- `compute_rapm.py`
		- `compute_xrapm.py`
		- `compute_xrapm_improved.py`
	- Pipeline docs/scripts updated to new paths (`readme.md`, `scripts/compute_data.sh`, `scripts/reproduce_pipeline.sh`, `reference/data_reproduction.md`, `.github/copilot-instructions.md`).
	- New modeling strategy document added: `reference/modelling_stats.md`.
	- Core decision:
		- RAPM/xRAPM remain computed in-house.
		- DARKO handled as fetched canonical external input (manual CSV ingest path, no stable public API).
		- New custom metric `BKE` defined as fused predictive impact model (RAPM + DARKO + linear/context signals), with role/value separation preserved.
	- Rationale: separates linear computed stats from model-based impact estimates and establishes explicit modeling stage boundaries.

- 2026-02-15 — Defensive archetypes v3.5: Rim Protectors extremely rare (anchor min 0.84, score coef 0.70; nearly all borderline go to Dropping/Mobile Bigs), Wing Stoppers much more common (difficulty threshold 0.40, score boost 1.12; increase mainly from Rotational Defender). Tuning only, logic/eligibility unchanged. All logic/thresholds parameterized. No eligibility violations. Ready for further tuning.

Project Context Summary — Offensive Archetypes v4 (auto-updated)
=================================================================

Purpose
-------
- This file captures the complete, project-relevant context from recent development on the offensive archetypes system (v1 → v2 → v3 → v4), viewer updates, data artifacts, and validation runs. Keep this file updated whenever you or an agent encounter important facts, thresholds, decisions, or outputs that affect classification logic, viewer behavior, or downstream evaluation.

Recent Update (2026-02-15)
--------------------------
- `app/player_data_viewer.py` now normalizes `PLAYER_ID` → `player_id` when loading parquet files, which fixed missing modal Position Estimate percentages (`pct_pg/pct_sg/pct_sf/pct_pf/pct_c`) that were previously rendering as `—`.
- Added position filter chips in the player data UI (`All Positions`, `Guard`, `Guard-Forward`, `Forward`, `Forward-Center`, `Center`) with client-side state in JS (`curPos`) integrated into `filterPlayers()`.
- `resetFilters()` now resets position chips to default (`All Positions`) in addition to existing search/dropdown/sort reset behavior.
- Regenerated `app/player_data.html`; detail payload now includes populated position estimate percentages.

Important Paths
---------------
- Workspace root: /mnt/c/Users/danie/OneDrive/Documents/BKE
- Main scripts and artifacts:
	- `src/data_compute/compute_player_archetypes.py` — v3 classification engine (percentile-based)
	- `app/player_archetype_viewer.py` — viewer generator (HTML + JS template)
	- `app/player_archetypes.html` — generated interactive viewer (open in browser)
	- `data/processed/player_archetypes.parquet` — latest classification output (1680 player-seasons)
	- `reference/player_offensive_archetypes.md` — v3 documentation (rewritten)
	- `loop/context_summary.txt` — this file (source of truth for session-level knowledge)
	- `/tmp/write_v3.py`, `/tmp/patch_v3.py`, `/tmp/patch_v3b.py` — generator/patch scripts used during development (kept as artifacts)

High-level Goals (from user)
----------------------------
1. Convert many static thresholds into percentile-based gates so the system remains valid across seasons.
2. Tighten Ball Dominant Creator (BDC) and All-Around Scorer pools to retain roughly 40–60% of prior sizes (aim ≤60% retention of BDCs).
3. Add BDC subtypes: Heliocentric Guard, Post Hub, Gravity Engine (priority ordering and robust gating).
4. Split PnR Big into PnR Rolling vs PnR Popping (based on FG3A per-36 relative to season median).
5. Treat computed USG% as a soft signal only (do not use as a hard gate).
6. Improve Connector by adding touches / sec-per-touch filters so only active creators are captured.
7. Relax Movement Shooter rule to movement/(movement+spotup) >= 0.4 instead of strictly movement > spotup.
8. Convert FG2A_RATE thresholds to percentiles (P70 for interior, P30 for perimeter) instead of hard 0.70/0.45.
9. Split a single confidence metric into `role_confidence` (fit) and `role_effectiveness` (production quality).
10. Keep the whole system reproducible and documented; provide viewer & docs updates.

Summary of Key Work Completed
-----------------------------
- Percentile computation implemented: `PCTILE_THRESHOLDS` (mapping threshold name -> (column, percentile)); `compute_season_percentiles()` resolves values per season from qualified players.
- New static constants placed under `STATIC` where percentiles are inappropriate (e.g., `MIN_MINUTES`, `POST_WEIGHT`, `HELIOCENTRIC_ON_BALL`, `POST_HUB_POSS`, `MOVEMENT_RATIO_MIN`, `CONNECTOR_SEC_PER_TOUCH_MAX`).
- v3 classification engine implemented in `src/data_compute/compute_player_archetypes.py` (written using `/tmp/write_v3.py` then patched with patch scripts):
	- BDC main path: requires ball_dom >= P80, AST >= P75, PTS >= P50, and composite >= 0.35 (composite uses linear interpolation from threshold → P95; 60% volume / 40% playmaking).
	- BDC subtypes priority: Post Hub (dominant/relative), Gravity Engine (high 3PA+eff), Heliocentric Guard (very high ball_dom P85), Primary Scorer fallback.
	- Hub (elite playmaker + elite scorer) applies the same subtype logic; additional Playmaking Hub sub-path (elite AST + POST >= 0.10, catches Sabonis).
	- Ballhandler: high playmaking, not high scorer.
	- Primary Scorers: ball_dom >= P60; FG2A percentile gating for interior/perimeter/all-around; All-Around requires pts >= P60 to reduce false positives.
	- Connector: AST >= P50 with activity filter (touches >= P50 OR sec-per-touch <= 2.5) and extra secondary-ast signals.
	- PnR Big: PRROLLMAN >= P70, choose Popping vs Rolling by FG3A >= P50 (median).
	- Off-Ball Finisher: CUT+PRROLL ≥ P75 or finish_score > 0.20.
	- Movement Shooter: movement >= P70 AND movement/(movement+spotup) >= 0.40.
	- Stationary Shooter: spotup >= P50.
	- Rotation Piece: catch-all.

Key Implementation Details
------------------------
- Composite calculation for BDC uses linear interpolation from floor (threshold) to P95 for ball_dom, scoring (PTS), and AST. This provides discrimination between borderline and elite players.
- `role_confidence` is fit (0–1) and typically derived from sqrt or normalized interpolations depending on archetype; `role_effectiveness` is a weighted function of TS z-score, volume (PTS/36), and relevant PPPs.
- USG is computed as a proxy when missing but only used as a soft signal. It is not a disqualifier.
- Movement Shooter ratio changed to movement/(movement+spotup) >= 0.4.
- FG2A_RATE is used via season percentiles: interior P70 / perimeter P30.

Representative 2024-25 Percentile Values (resolved during the run)
----------------------------------------------------------------
(These are per `compute_season_percentiles()` for qualified players; include as historic reference)
- BD_MIN (P80): 0.3720
- PLAYMAKER (AST_PER36 P75): 5.0741
- HIGH_SCORING (PTS_PER36 P70): 18.95
- FG2A_INTERIOR (P70): 0.6731
- FG2A_PERIMETER (P30): 0.4781
- CONNECTOR_AST (P50): 3.3759
- ALL_AROUND_SCORING (P60): 17.2264
- HIGH_PRROLLMAN (P70): 0.0646
- FG3A_MEDIAN (P50): 5.8126
- TOUCHES_MEDIAN (P50): 40.3
- HIGH_FG3A (P80): ~8.017

Distribution & Validation (2024-25 qualified players)
--------------------------------------------------
- Qualified: 329 players (MIN ≥ 500, GP ≥ 20, MPG ≥ 15)
- Archetype counts (v3 run):
	- Off-Ball Stationary Shooter: 64 (19.5%)
	- Off-Ball Finisher: 62 (18.8%)
	- Ball Dominant Creator: 40 (12.2%)
	- Ballhandler: 31 (9.4%)
	- Connector: 28 (8.5%)
	- All-Around Scorer: 26 (7.9%)
	- PnR Rolling Big: 21 (6.4%)
	- Interior Scorer: 18 (5.5%)
	- Off-Ball Movement Shooter: 17 (5.2%)
	- Rotation Piece: 10 (3.0%)
	- Perimeter Scorer: 7 (2.1%)
	- PnR Popping Big: 5 (1.5%)

Subtype counts (examples): Transition Player 45, High Volume 33, Heliocentric Guard 29, Gravity Engine 6, Post Hub 2.

Notable Player Classifications (validated)
-----------------------------------------
- LeBron James → Ball Dominant Creator / Heliocentric Guard (conf ~100%, eff ~85%)
- Stephen Curry → Ball Dominant Creator / Gravity Engine (100%, ~86%)
- Nikola Jokić → Ball Dominant Creator / Post Hub (100%, ~98%)
- Domantas Sabonis → Ball Dominant Creator / Post Hub (96%, ~95%)
- Luka Dončić → Ball Dominant Creator / Heliocentric Guard (100%, ~85%)
- Tyrese Haliburton → Ball Dominant Creator / Gravity Engine (100%, ~88%)
- Kevin Durant → All-Around Scorer / High Volume (100%, ~92%)
- Anthony Edwards → All-Around Scorer / High Volume (100%, ~80%)
- Brook Lopez → PnR Rolling Big (100%, ~82%)
- Many others validated; full list printed during run.

Viewer Updates
--------------
- `app/player_archetype_viewer.py` updated to show `Fit: X% · Eff: Y%` by:
	- Adding `off_effectiveness` to the player dict (p.off_effectiveness derived from `role_effectiveness` scaled to percent).
	- Replacing the old confidence template snippet with `Fit: ${p.off_confidence}% · Eff: ${p.off_effectiveness}%${p.off_secondary ? ' · ' + p.off_secondary : ''}`.
	- Adding new sort button labels: `Fit` and `Effectiveness` to allow client-side sorting by either metric.
- HTML regenerated: `app/player_archetypes.html` created/updated and contains references to `off_effectiveness` and PnR archetypes. Note: client-side JS renders template literals; grep for literal strings like `Fit:` may not show in server-side file counts.

Bugs/Issues Encountered & Fixes
--------------------------------
- Unicode middot (·, U+00B7) in the template prevented naive search/replace; resolved using exact string replacement and updating the template literal.
- Syntax indentation error in viewer patch (extra blank line before `elif`) — fixed by correcting indentation.
- `POST_HUB_POSS` had a unicode arrow in an earlier comment which caused a KeyError during string matching in patches; fixed by avoiding matching on the arrow and replacing a shorter safe substring.
- v3 code was initially present only in `/tmp` scripts; `write_v3.py` used to persist the v3 file at `src/data_compute/compute_player_archetypes.py`, then patches applied (`patch_v3.py`, `patch_v3b.py`/fixed).

Commands & Tools Used (repro)
----------------------------
Run classifier (v3) and regenerate parquet:
```bash
.venv/bin/python src/data_compute/compute_player_archetypes.py
```

Generate viewer HTML:
```bash
.venv/bin/python app/player_archetype_viewer.py
```

Check python syntax:
```bash
.venv/bin/python -m py_compile src/data_compute/compute_player_archetypes.py
.venv/bin/python -m py_compile app/player_archetype_viewer.py
```

Quick verification script used during this session:
```bash
.venv/bin/python /tmp/final_verify.py
```

Files you should version-control (recommended)
-------------------------------------------
- `src/data_compute/compute_player_archetypes.py` (v3 source)
- `app/player_archetype_viewer.py` (viewer generator)
- `reference/player_offensive_archetypes.md` (documentation)
- `/tmp/write_v3.py`, `/tmp/patch_v3.py`, `/tmp/patch_v3b_fixed.py` (developer artifacts — consider moving into `scripts/` if you want to reuse)

Pending / Future Work
---------------------
- Small tuning: BDC retention target aimed ≤60% but actual is ~61.5% (40/65 → 61.5%); acceptable but can be tuned further by adjusting BD_MIN (P80→P85) or composite threshold.
- Add unit tests and CI checks for: percentile resolver, composite interpolation, key player expectations, and viewer HTML generation.
- Update `reference/player_offensive_archetypes.md` further with more examples and the exact per-season percentile table output for each run (optionally commit snapshot tables).
- Add a small CLI or Makefile to: (1) run classifier, (2) regenerate viewer, (3) open HTML in the system browser (for convenience).
- Consider moving the `/tmp` generator/patch scripts into a `dev_tools/` or `scripts/` folder and adding usage notes.

How to keep this file updated
-----------------------------
- Purpose: this is the single-session/ongoing context summary; append important discoveries, thresholds, distribution changes, and decision justifications here.
- When you or the agent: change classification logic, re-tune percentiles, fix viewer templates, or regenerate `player_archetypes.parquet`, add a short entry with date-stamped notes including:
	- What changed (file & lines),
	- Why it changed (bugfix, tuning, design decision),
	- Impact on outputs (counts, key players),
	- Commands to reproduce.
- Use the following template for additions (append to the end of this file):
```
- YYYY-MM-DD — Short title
	- Files changed: path(s)
	- Summary: what & why
	- Repro commands:
		```bash
		# commands
		```
	- Observed impact:
```

Session History (high level)
---------------------------
- v1 → v2: fixed INTERIOR_RATIO, USG/TS NaN handling, playtype & per-36 normalization, initial archetype layout.
- v2 → v3 (this session): implemented percentile thresholds, BDC subtypes, composite interpolation, PnR Big split, connector filters, movement shooter relax, dual confidence, regenerated parquet, updated viewer and docs.

Contact / Ownership
-------------------
- Primary developer: you (workspace owner). The repository contains recorded scripts and the v3 compute script. Use this file as the first place to look for session-specific rationales.

End of summary (last updated: 2026-02-07)

Note: Update this file whenever you encounter important changes to thresholds, classification rules, viewer templates, or data artifacts. Add date-stamped entries using the template above.

- 2026-02-07 — Archetype v3 incremental updates / 3.5 prep
	- Files changed: src/data_compute/compute_player_archetypes.py, app/player_archetype_viewer.py, app/player_archetypes.html, data/processed/player_archetypes.parquet
	- Summary: 
		- Viewer: Added explicit `PnR Rolling Big` and `PnR Popping Big` filter/options and updated human-readable reasons to reference percentile-based decisions (v3 language).
		- Classification: Confirmed v3 percentile-based thresholds are active (`PCTILE_THRESHOLDS` + `compute_season_percentiles()`); `role_confidence` (fit) and `role_effectiveness` (production) present in output. Regenerated `player_archetypes.parquet` and `player_archetypes.html`.
		- Rotation Piece analysis: Performed a cascade trace for all Rotation Piece players (25 total across 2022-25). Identified three root causes that allow stars to fall into `Rotation Piece`:
			1. Players who are both playmakers and high scorers are excluded by mutually-exclusive gates (BDC requires composite, Hub requires ELITE thresholds, Ballhandler excludes high scorers).
			2. All-Around Scorer requires P60 scoring gate which drops some legit mid/high scorers.
			3. Missing/zero playtype data or weak off-ball signals lead to the catch-all assignment.
		- Short proposal (low-risk changes) to eliminate the Rotation Piece leak:
			1. Add a `is_playmaker && is_high_scorer` path → classify as `All-Around Scorer` before Ballhandler (catches playmaker+scorer cases).
			2. Lower/relax the All-Around scoring gate (P60→P50) or add a fallback All-Around at P50.
			3. Replace the unconditional `Rotation Piece` catch-all with a nearest-fit fallback that assigns the archetype whose normalized distance to its gate is smallest (low confidence), and treat obviously missing playtype rows specially.
		- Midrange / Floater plan: Proposed to fetch shot-zone data (preferred) from NBA shot-zone endpoints (Restricted Area / Paint / Midrange / 3PT zones) and create `MIDRANGE_FREQ` and `AT_RIM_FREQ`. This will allow: (a) a `Midrange Scorer` subtype/archetype, (b) better separation of rim finishers vs midrange specialists (floaters), and (c) more meaningful FG2A usage splits.
	- Repro commands:
		```bash
		# Re-run classifier (v3) and regenerate parquet
		.venv/bin/python src/data_compute/compute_player_archetypes.py
		# Regenerate viewer HTML
		.venv/bin/python app/player_archetype_viewer.py
		# Quick syntax checks
		.venv/bin/python -m py_compile src/data_compute/compute_player_archetypes.py
		.venv/bin/python -m py_compile app/player_archetype_viewer.py
		```
	- Observed impact:
		- Viewer now exposes PnR Rolling/Popping filters and percentile language in player reasons (verified for LeBron, Porziñģis, others).
		- No live regressions observed; HTML regenerated and parquet updated (1680 player-seasons). Rotation Piece count (qualified 2024-25) was 10 — proposed tweaks would reclassify these into concrete archetypes.

- Next steps (recommended):
	- Grand Unified Model: Implement compute_rapm_metrics.py to unify Win Shares, BPM, RAPM outputs into a single EPM-like metric (Layer 3).
	- Add unit tests for v4 classifier: percentile resolver, shot zone merge, key player expectations.
	- Consider adding more midrange/shot zone stats to the viewer cards.

- 2025-06-09 — Archetype v4: Shot zones, Rotation Piece elimination, Midrange Scorer
	- Files changed: src/data_fetch/fetch_shot_zones.py (NEW), src/data_compute/compute_player_archetypes.py (MAJOR), app/player_archetype_viewer.py, app/player_archetypes.html, data/processed/player_archetypes.parquet
	- Summary:
		- NEW: Created `src/data_fetch/fetch_shot_zones.py` — fetches shot zone data from NBA.com `LeagueDashPlayerShotLocations` endpoint (league-wide, single API call per season). Computes AT_RIM_FREQ, MIDRANGE_FREQ, PAINT_FREQ, CORNER3_FREQ, AB3_FREQ, AT_RIM_PLUS_PAINT_FREQ. Saves to `data/tracking/{season}/shot_zones.parquet`.
		- Shot zone data fetched for 3 seasons: 2022-23 (539 players), 2023-24 (572), 2024-25 (569)
		- API response format: `resultSets` is a dict (not list), 6 base columns (PLAYER_ID, PLAYER_NAME, TEAM_ID, TEAM_ABBREVIATION, AGE, NICKNAME), 8 zones × 3 stats (FGM, FGA, FG_PCT) = 30 zone columns
		- Zone prefixes: RA (Restricted Area), PAINT (In The Paint Non-RA), MR (Mid-Range), LC3, RC3, AB3, BC, C3 (Corner 3)
		- v4 classifier changes:
			1. Added 4 new PCTILE_THRESHOLDS: HIGH_AT_RIM (P70), HIGH_MIDRANGE (P70), LOW_MIDRANGE (P30), MODERATE_MIDRANGE (P50)
			2. Added `load_shot_zone_data(season)` and pass to `compute_archetype_features()`
			3. NEW Step 2: All-Around Scorer inserted BEFORE Ballhandler — if playmaker AND high scorer → All-Around Scorer (with Midrange Scorer subtype if high midrange)
			4. Ballhandler moved to Step 3
			5. Interior Scorer subtypes: "Rim Finisher" (AT_RIM_FREQ >= P70), "Midrange Scorer" (MIDRANGE_FREQ >= P50), "High Volume" (fallback)
			6. All-Around gate lowered: uses MODERATE_SCORING (P50) instead of ALL_AROUND_SCORING (P60)
			7. ROTATION PIECE ELIMINATED: Replaced with best-fit fallback scoring each candidate archetype and picking the best match. True last resort uses scoring/defensive signals.
		- Viewer updated: removed Rotation Piece from dropdown, added PnR bigs, added AT_RIM% & MID% to player card stats, updated offensive reasons for new subtypes
	- Validated against key players:
		- DeRozan: Interior Scorer / Midrange Scorer (MR=0.459) ✓
		- Giannis: BD Creator / Offensive Hub (AT_RIM=0.571) ✓ (BD Creator since high ball dominance)
		- Curry: BD Creator / Gravity Engine (all 3 seasons) ✓
		- KD: All-Around / High Volume (2 of 3); Interior Scorer / Midrange Scorer in 23-24 ✓
		- Embiid: Interior Scorer / Midrange Scorer in 22-23; Insufficient Minutes in 24-25 ✓
		- SGA: BD Creator / Heliocentric Guard (all 3) ✓
		- Ant Edwards: All-Around Scorer / High Volume (all 3) ✓
	- Distribution (2024-25 qualified, 329 players):
		- Off-Ball Stationary Shooter: 63 (19.1%)
		- Off-Ball Finisher: 62 (18.8%)
		- All-Around Scorer: 40 (12.2%)
		- Ball Dominant Creator: 40 (12.2%)
		- Connector: 31 (9.4%)
		- Ballhandler: 31 (9.4%)
		- PnR Rolling Big: 20 (6.1%)
		- Interior Scorer: 18 (5.5%)
		- Off-Ball Movement Shooter: 12 (3.6%)
		- Perimeter Scorer: 7 (2.1%)
		- PnR Popping Big: 5 (1.5%)
		- Rotation Piece: 0 (0.0%) ← ELIMINATED
	- Subtype totals (all seasons): Transition Player 98, High Volume 85, Heliocentric Guard 80, Elite Shooter 77, Midrange Scorer 42, Playmaking Big 41, Gravity Engine 19, Rim Finisher 14, Post Hub 7, Primary Scorer 7, Offensive Hub 4, Perimeter Defender 2, Rebounder 1
	- Repro commands:
		```bash
		# Fetch shot zones (one-time, cached)
		.venv/bin/python src/data_fetch/fetch_shot_zones.py
		# Run v4 classifier
		.venv/bin/python src/data_compute/compute_player_archetypes.py
		# Regenerate viewer
		.venv/bin/python app/player_archetype_viewer.py
		```
	- New data files:
		- data/tracking/2022-23/shot_zones.parquet
		- data/tracking/2023-24/shot_zones.parquet
		- data/tracking/2024-25/shot_zones.parquet
		- data/tracking_cache/shot_locations_{season}.json

v4.1 changes (applied 2026-02-07):
	- Alternate Interior Scorer entry path (low BD variant using FG2A + AT_RIM_PLUS_PAINT >= P60)
	- Midrange subtype split: P70+ = Midrange Scorer, P50-P70 = Midrange Lean
	- Frozen FALLBACK_VECTORS for best-fit fallback (prevents feature creep)
	- AT_RIM_PAINT_P60 added to percentile thresholds
	- Validated: Interior Scorer 35→38→37 across seasons; Midrange Lean subtype appearing correctly; all star classifications stable

v4.2 changes (applied 2026-02-07):
	- Skill-finishing gate for Interior Scorer: non-stars must show paint_freq + midrange_freq >= 0.25 (SKILL_FINISHING_MIN). Rim-dependent players (only dunks/layups) fall through to PnR Big / Off-Ball Finisher.
	  - Redirected: Onyeka Okongwu → PnR Rolling Big, Christian Braun → Off-Ball Finisher, Jeremy Sochan → Connector, Jalen Duren → Connector
	- Off-Ball Movement Shooter thresholds loosened: P70→P60, MOVEMENT_RATIO_MIN 0.40→0.30. Captures Tim Hardaway Jr., KCP, Huerter, Moses Moody, Dalton Knecht, etc.
	- New Step 7b: Perimeter Scorer (moderate BD). BD >= P50 + scoring >= P25. Catches self-creating wings (Terry Rozier, Cam Whitmore, Brandon Miller, Derrick White, etc.)
	- Distribution (2024-25): Stationary 64→45 (70%), Movement 12→26, Perimeter 7→14, Interior 37→33, PnR Rolling 12→13
	- All star classifications stable. Viewer regenerated. Markdown docs updated to v4.2.

v4.3 changes (applied 2026-02-07):
	- PnR Big absorption: Added "prominent" path (PRROLLMAN_POSS_PCT >= P50 + interior profile). PnR Roll Man no longer needs to be the MOST COMMON play; as long as it's prominent and the player has an interior profile, they're classified as PnR Big. Also added redirect inside Off-Ball Finisher step for bigs with PnR activity.
	  - Key absorptions: Jarrett Allen, Daniel Gafford → PnR Rolling Big (from Interior Scorer); many bigs from Off-Ball Finisher → PnR Rolling Big
	- Harsher Interior Scorer skill check: SKILL_FINISHING_MIN raised from 0.25 to 0.40. Rim-runners with prominent PnR + low midrange always fall through regardless of scoring volume.
	- "Midrange Lean" subtype renamed to "Inside-the-Arc" everywhere
	- HTML viewer: Added "Reset All" button to clear all filters
	- Archetype Embedding (NEW): Soft role model producing confidence-weighted 11-dimensional embedding vector per player.
	  - Algorithm: (1) compute raw scores for all 11 archetypes using uncapped gate math; (2) convert to confidence via exp(-α·distance); (3) build L1-normalized embedding.
	  - Output: data/processed/archetype_embeddings.parquet (1,680 embeddings), also merged into player_archetypes.parquet.
	  - Derived metrics: emb_entropy (hybrid score), emb_dominance (purity score), emb_entropy_norm (normalized hybrid).
	  - Examples: LeBron 17% dominance/94% hybrid; Curry 43%/75%; Jarrett Allen 35%/48%.
	- Distribution (2024-25): PnR Rolling 13→40, Off-Ball Finisher 54→36, Interior Scorer 33→19, Connector 28→29, Perimeter 14→17
	- All star classifications stable. Reference docs updated to v4.3.

- 2026-02-13 — Defensive archetypes v3.1 (role-only decision flow + hard caps)
	- Files changed: `src/data_compute/compute_defensive_archetypes_v2.py`, `reference/player_defensive_archetypes.md`, `readme.md`
	- Summary:
		- Removed impact from role assignment; archetype stage is now behavior-only.
		- Implemented 3-layer classifier: size-band gating (Guard/Wing/Big) → behavior indices → explicit decision tree.
		- Added hard rarity caps per season: `POA Defender <= 12%`, `Versatile Defender <= 8%` with deterministic demotions.
		- Added explicit fallback roles: `Rotational Defender`, `Rotational Big`.
		- Preserved impact outputs (`defensive_effectiveness`, `defensive_fit`) as post-classification overlay only.
		- Added impact report outputs: `data/processed/defensive_archetypes_v2_impact_report.csv/.txt`.
	- Repro commands:
		```bash
		.venv/bin/python src/data_compute/compute_defensive_archetypes_v2.py
		python3 - <<'PY'
		import pandas as pd
		print(pd.read_csv('data/processed/defensive_archetypes_v2_impact_report.csv').head())
		PY
		```
	- Observed overall impact (baseline vs v3.1, qualified pool):
		- Rotational Defender: 27.65% → 40.88% (+13.23 pp)
		- Low-Activity Defender: 10.69% → 20.44% (+9.75 pp)
		- Off-Ball Chaser: 15.01% → 17.98% (+2.97 pp)
		- POA Defender: 5.94% → 6.19% (+0.25 pp)
		- Versatile Defender: 0.00% → 0.25% (+0.25 pp)
		- Rotational Big: 21.46% → 5.17% (-16.29 pp)
		- Mobile Big: 11.37% → 0.34% (-11.03 pp)

- 2026-02-13 — Defensive archetypes v3.2 (confidence-scored routing + margin rule)
	- Files changed: `src/data_compute/compute_defensive_archetypes_v2.py`, `reference/player_defensive_archetypes.md`, `readme.md`
	- Summary:
		- Replaced hard-threshold branch routing with in-band role confidence scoring and argmax assignment.
		- Added stability margin rule: if top-two role scores are within 0.035, assign rotational role.
		- Removed hard-cap dependency from assignment path; role balance now emerges from score competition and eligibility anchors.
		- Tuned POA anchor gating (composite + ball pressure + screen navigation), widened versatile eligibility for high multi-signal wings, and rebalanced big-role competition to reduce Mobile suppression.
	- Repro commands:
		```bash
		.venv/bin/python src/data_compute/compute_defensive_archetypes_v2.py
		```
	- Observed overall distribution (qualified pool, v3.2 final run):
		- Rotational Defender: 310 (26.3%)
		- Low-Activity Defender: 219 (18.6%)
		- Off-Ball Chaser: 167 (14.2%)
		- POA Defender: 154 (13.1%)
		- Rim Protector: 126 (10.7%)
		- Versatile Defender: 83 (7.0%)
		- Mobile Big: 57 (4.8%)
		- Rotational Big: 55 (4.7%)

- 2026-02-14 — Defensive archetypes v3.2 detailed role competition pass (no caps)
	- Files changed: `src/data_compute/compute_defensive_archetypes_v2.py`, `reference/player_defensive_archetypes.md`, `readme.md`, `data/processed/defensive_archetypes_v2.parquet`, `data/processed/defensive_archetypes_v2.csv`, `data/processed/defensive_archetypes_v2_impact_report.csv`, `data/processed/defensive_archetypes_v2_impact_report.txt`
	- Summary:
		- Implemented deterministic size-band routing exactly per spec: Big (`pct_centers>=0.50` or `height>=81`), Guard (`pct_guards>=0.60`), else Wing.
		- Replaced branch-routing with in-band role score competition using anchor-eligible roles only.
		- Updated margin rule to `0.05` and added low-activity override (`engagement<0.25` and `top_score<0.65`).
		- Confidence now uses role separation scale: `0.5 + 0.5*(top-second)`.
		- Removed artificial rarity caps from assignment path.
	- Repro commands:
		```bash
		.venv/bin/python src/data_compute/compute_defensive_archetypes_v2.py
		python3 - <<'PY'
		import pandas as pd
		report = pd.read_csv('data/processed/defensive_archetypes_v2_impact_report.csv')
		print(report.to_string(index=False))
		PY
		```
	- Observed impact vs `defensive_archetypes_v2_baseline_pre_v31.parquet` (2024-25 qualified: MIN>=500, GP>=20, MPG>=15):
		- Changed archetypes: `220 / 329` players (`66.87%`)
		- POA Defender: `21 -> 41` (`+95.24%`)
		- Wing Stopper: `0 -> 13` (`+100%` from none)
		- Versatile Defender: `0 -> 16` (`+100%` from none)
		- Low-Activity Defender: `18 -> 37` (`+105.56%`)
		- Rotational Big: `75 -> 41` (`-45.33%`)
		- Mobile Big: `33 -> 1` (`-96.97%`)

- 2026-02-14 — Defensive archetypes v3.3 (remove Rotational Big)
	- Files changed: `src/data_compute/compute_defensive_archetypes_v2.py`, `reference/player_defensive_archetypes.md`, `readme.md`, `data/processed/defensive_archetypes_v2.parquet`, `data/processed/defensive_archetypes_v2.csv`, `data/processed/defensive_archetypes_v2_impact_report.csv`, `data/processed/defensive_archetypes_v2_impact_report.txt`
	- Summary:
		- Removed `Rotational Big` from assignment path.
		- Big-band players now classify via big-role competition only (`Rim Protector`, `Dropping Big`, `Mobile Big`), with low-activity override retained.
		- Margin fallback remains active for `Rotational Defender` in guard/wing bands.
	- Repro commands:
		```bash
		.venv/bin/python src/data_compute/compute_defensive_archetypes_v2.py
		```
	- Observed overall distribution (all qualified seasons):
		- Rotational Defender: 341
		- Low-Activity Defender: 287
		- Rim Protector: 163
		- Off-Ball Chaser: 159
		- POA Defender: 131
		- Wing Stopper: 40
		- Mobile Big: 22
		- Versatile Defender: 20
		- Dropping Big: 16
		- Rotational Big: 0

- 2026-02-14 — Position estimate pipeline (lineup height ranking) + defensive size-band upgrade
	- Files changed: `src/data_compute/compute_position_estimate.py`, `src/data_compute/compute_defensive_archetypes_v2.py`, `readme.md`
	- Summary:
		- Added new compute stage to produce season-level position estimates from 5-man lineup height ordering on possessions.
		- New output includes `pct_pg/pct_sg/pct_sf/pct_pf/pct_c` and grouped own-role shares (`pct_guards_own`, `pct_forwards_own`, `pct_centers_own`) per player-season.
		- Added robust fallback for missing lineup coverage: bio-prior distribution from `primary_position`.
		- Defensive archetype v2 now loads `data/processed/player_position_estimates.parquet` and uses own-position shares + height + estimated primary position for size-band routing.
		- This replaces dependency on trivial `players.parquet` primary position for size-band decisions.
	- Repro commands:
		```bash
		python3 src/data_compute/compute_position_estimate.py
		python3 src/data_compute/compute_defensive_archetypes_v2.py
		```
	- Notes:
		- Direct BRef scraping is currently blocked in this environment (Cloudflare 403), so the implementation uses an internal BRef-style lineup-ranking method.

- 2026-02-14 — Position estimate output contract updated to per-season files + frontend display block
	- Files changed: `src/data_compute/compute_position_estimate.py`, `src/data_compute/compute_defensive_archetypes_v2.py`, `app/player_data_viewer.py`, `readme.md`
	- Summary:
		- `compute_position_estimate.py` now writes one output per season:
			- `data/processed/player_position_estimates_2022-23.parquet/.csv`
			- `data/processed/player_position_estimates_2023-24.parquet/.csv`
			- `data/processed/player_position_estimates_2024-25.parquet/.csv`
		- Kept combined compatibility artifact (`player_position_estimates.parquet/.csv`) for downstream safety.
		- Defensive archetype loader now prioritizes seasonal files via glob and falls back to combined file.
		- Frontend player data viewer now loads seasonal position estimate files and shows a dedicated modal section: **Position Estimate** with PG/SG/SF/PF/C percentages and estimate method.
	- Repro commands:
		```bash
		python3 src/data_compute/compute_position_estimate.py
		python3 src/data_compute/compute_defensive_archetypes_v2.py
		python3 app/player_data_viewer.py
		```
- **NEW:** Salary fetcher (`src/data_fetch/fetch_player_salaries.py`) scrapes ESPN, outputs per-season parquet files (`player_salaries_2022-23.parquet`, etc). Viewer now loads these and displays salary in the player modal subtitle for the correct season.

2026-03-05 — Simulation Core Step 2 Sprint (clutch fetch + lineup model + frontend tab)
- Scope completed across 3 requested steps:
	- Step 1: clutch fetch + aggregate integration
	- Step 2: lineup projection model with validation outputs
	- Step 3: simulation viewer frontend upgrade (new Step 2 tab with clickable team cards)
- Files added:
	- `src/data_fetch/fetch_player_clutch_stats.py`
	- `src/simulation/lineup_projection.py`
- Files updated:
	- `src/player_eval/constants.py`
	- `src/profile_aggregate/build_profile_aggregate.py`
	- `src/simulation/simulation_config.py`
	- `app/simulation_viewer.py`
	- `readme.md`
	- `reference/simulation/Simulation_Core_Summary.md`
- Clutch fetch validation (runtime):
	- 2022-23: 470 rows
	- 2023-24: 474 rows
	- 2024-25: 504 rows
	- Combined: 1448 rows (`data/historical/player_clutch_stats_all.parquet`)
- Aggregate rebuild validation:
	- `aggregate/player_profile_aggregate.parquet`: 1971 rows x 924 cols
	- `reports/profile_aggregate_validation.json` includes clutch group + `clutch_minutes` coverage `0.7346524606798579`
- Step 2 lineup model validation snapshot (`python3 src/simulation/lineup_projection.py`):
	- team-seasons: 90
	- starter overlap mean: 3.556/5 (0.7111)
	- clutch overlap mean: 4.289/5 (0.8578)
	- rotation corr: 0.5367
	- outputs written:
		- `data/processed/simulation/simulation_step2_lineup_profiles.parquet`
		- `reports/simulation_step2_lineup_profiles.json`
		- `reports/simulation_step2_validation.json`
- Frontend:
	- `python3 app/simulation_viewer.py` regenerated `app/simulation.html`
	- Step 1 and Step 2 views both available in one standalone page.

2026-03-05 — Position-band impact audit + minimal-impact implementation (frontend/backend)
- Scope: evaluate each position-band audit item for real impact on current metrics/models, implement only low-risk items, reproduce outputs, and defer high-impact items pending approval.
- Implemented low-impact changes:
	- `src/simulation/lineup_projection.py`: robust hybrid role parsing + `position_band` carried in Step 2 player payloads.
	- `app/simulation_viewer.py`: player list now shows `role · position_band`.
	- `src/modeling/model_config.py`: canonical position labels listed first in `position_buckets` with legacy aliases retained for compatibility.
	- `src/data_compute/compute_defensive_archetypes.py`: explicit legacy warning banner pointing to v2 script.
- Exposure findings used for change classification:
	- Decomposition hybrid-collapse exposure: `198/198` hybrid rows collapsed in `bke_v28_decomposition.parquet` (`Forward-Center` and `Guard-Forward` aliases mapped to non-hybrid buckets).
	- Step 2 fallback exposure: `0/1680` rows missing role mapping, so fallback risk is effectively zero in current data.
- Reproduction / drift checks:
	- `python3 src/simulation/lineup_projection.py` rerun: unchanged metrics (`starter_overlap_rate_mean=0.7111`, `clutch_overlap_rate_mean=0.8578`, `rotation_correlation=0.5367`).
	- `python3 app/simulation_viewer.py` rerun: `app/simulation.html` regenerated.
	- `python3 src/modeling/construct_bke_scores_v27.py` rerun: raw file MD5 changes per run because payload includes `generated_at`; stable-content MD5 (excluding `generated_at`) is unchanged (`d5588c20dcdb083d6b9724e50982b87d`).
- Deferred high-impact items (require explicit user approval before implementation):
	- Preserve hybrid bands end-to-end in decomposition cohorting (would change grouped percentile baselines and ranks).
	- Matchup/position taxonomy redesign across modeling inputs.
