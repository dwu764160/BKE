# Claude Code self-improvement configuration for BKE (Basketball KPI Engine)
# Edit thresholds based on observed signal quality after real sessions.
# All scripts source this file before running.

# ── Verification checks (Python/BKE pipeline) ──────────────────────────────────
# Each entry: name|file_regex|root_marker|command|error_code_regex
# - file_regex: bash regex matched against the edited/modified file path
# - root_marker: "project" = repo root; or filename to search upward for
# - error_code_regex: optional; pattern used to extract error codes for trends
CHECK_DEFINITIONS=(
  "pytest|\\.py$|project|pytest -q tests/ 2>&1|FAILED|ERROR"
)

# Command execution defaults
CHECK_TIMEOUT_SECS=60
CHECK_OUTPUT_LINES=50

# ── Error detection ────────────────────────────────────────────────────────────
ERROR_THRESHOLD=2
POST_EDIT_RATE_LIMIT_SECS=60

# ── Pattern detection ──────────────────────────────────────────────────────────
PATTERN_ERROR_CODE_MIN=2
PATTERN_FILE_ERROR_MIN=3
SKILL_GAP_ESCALATE_MIN=2

# ── Notification priorities ────────────────────────────────────────────────────
SMOKE_FAIL_NOTIFY_PRIORITY=high
SKILL_GAP_NOTIFY_PRIORITY=urgent
